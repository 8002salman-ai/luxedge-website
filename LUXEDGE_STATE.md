# LUXEDGE STATE — Agent Handoff

> **MANDATORY:** Before any Luxedge work, read `LUXEDGE_MASTER_PLAN.md` first.
> `LUXEDGE_MASTER_PLAN.md` defines mission, business North Star, autonomy rules, and anti-drift architecture.
> `LUXEDGE_STATE.md` defines current technical implementation/state.
> If a task conflicts with the Master Plan, report the conflict instead of silently changing direction.

**Master Plan V1.1 installed:** YES — `LUXEDGE_MASTER_PLAN.md` at repository root (2026-08-17, verbatim canonical content). It is the WHY / North Star / anti-drift constitution; this file is the WHAT / current technical state. Read BOTH before any Luxedge work.

> ## CURRENT PRIORITY (owner decision, 2026-08-18)
> **PRODUCT → QUALIFICATION → LISTING → CHECKOUT → SALES.**
> **GOOGLE ADS LIVE DEMAND = DEFERRED / NOT A LAUNCH BLOCKER.** The Google Ads account currently requires payment/billing setup before the advertiser account can proceed. The owner will enable Google Ads + billing LATER, after Luxedge starts generating sales/revenue. DO NOT add payment, DO NOT add a card, DO NOT create a campaign, DO NOT spend on Google Ads. The Google Ads integration code built in Phases 4G/4G.1/4G.1A is **PRESERVED / READY FOR LATER** (server-side only, no credentials, zero live calls).
> Product discovery continues with the existing real non-Google evidence ONLY: CJ supplier data, retailer exact PDPs, real price/availability/shipping, identity-honest ratings/reviews, supplier reliability, margin, risk, supply visibility. Search-demand fields stay UNKNOWN when unavailable — never invented.

Updated **2026-08-18** after **PHASE 4H — ELEVATED DOG SOFA BED EVIDENCE PASS + GOOGLE ADS DEFERRED** on `luxedge-v2` — see the Phase 4H section below.

Updated **2026-08-17** after **PHASE 4C LIVE CJ CONFIG + FIRST WINNER SEARCH** on `luxedge-v2` — migrations **0008 (durable CJ point ledger) and 0009 (revoke ledger write grants) are both APPLIED ✅ and live-verified** (RPCs admin-only 400 P0001; CHECK constraints 23514; anon direct write → 401 42501 with grant-revoke hint; cleanups done). The owner-supplied `CJ_API_KEY` is configured in the Vercel **PREVIEW** env only (Sensitive, branch luxedge-v2, value never printed/committed; **Production untouched**). Real preview serverless proxy verified: **CJ Supplier API health = ONLINE** (real CJ auth). Two grounded DeepSeek Market Intelligence passes ran on real evidence: `pet enrichment` 45/100 and `dog travel accessories` 55/100 — **both below the 60 market gate ⇒ CJ product search NOT run, ZERO CJ product points consumed, QUALIFIED WINNER = NONE (bottleneck: market-opportunity evidence)**. Thresholds unchanged (Product ≥75 / Market ≥60), SKU→sizes honesty kept, fail-closed durable ledger enforced, query provenance verified against persisted MI recommendations. 274 tests passed + 1 skipped (guarded live harness). **MIGRATION 0008 = APPLIED ✅ · MIGRATION 0009 = APPLIED ✅ · LIVE CJ SUPPLIER INTEGRATION (auth/health) = PASS · LIVE CJ PRODUCT SEARCH = NOT RUN (market gate) · QUALIFIED BUSINESS WINNER = NONE · CJ_API_KEY = CONFIGURED (Preview only).**

## Phase 3B FINAL STATUS = PASS (closed)

- **Branch:** `luxedge-v2` (development). `main` = production (`luxedge.us`), untouched (`4d0086d`), NOT deployed from this branch.
- **Migrations applied to the REAL Supabase project (owner, Dashboard → SQL Editor):**
  - `0004_reconcile_live.sql` — applied ✅ (reconciled the legacy 13-table DB to the V2 schema)
  - `0005_drop_legacy_currency_checks.sql` — applied ✅ (dropped stale legacy `%_currency_check` / `%_country_check` constraints)
  - `0006_products_status_allow_draft.sql` — applied ✅ (adds `'draft'` to `products_status_check`; verified live — Phase 4A and Phase 4B product-draft writes with `status='draft'` succeed)
  - `0007_market_intelligence_job_type.sql` — **applied ✅ (owner, Dashboard → SQL Editor)** (adds `'MARKET_INTELLIGENCE'` to `agent_jobs_type_check`; verified live — a MARKET_INTELLIGENCE `agent_jobs` row now persists without the previous 23514 constraint error)
- **Live schema (verified):** **30 V2 tables** all present (customers, suppliers, supplier_products, supplier_variants, supplier_shipping, reviews, ai_providers, agent_jobs, agent_runs, agent_logs, product_candidates, product_scores, creative_assets, creative_jobs, campaigns, ad_creatives, ad_performance, + others) — MISSING: NONE — plus **5 preserved legacy tables** (profiles, admin_email_allowlist, supplier_mappings, price_history, processed_webhook_events) = 35 total.
- **Uppercase USD V2 write verified live:** after 0005, a products write with `currency='USD'` passes (previously 23514 on `products_currency_check`).
- **KONG product normalized:** the one real product now stores `currency='USD'` (PATCH verified, HTTP 200).
- **RLS / grants verified live (probes):** anon reads published catalog only; anon/buyer writes to products/customers/suppliers → 42501 blocked; buyer sees only own `customers` row; admin full access; service role works. RLS enabled and enforced.
- **Customer sync verified live:** real non-admin buyer → sign-in (no admin role claim) → `customers` row created via the user's own JWT with matching `auth_user_id` → own-row isolation → admin-table blocked → refresh survival → logout. Test buyer + row deleted afterward.
- **Real product E2E verified:** **KONG Classic, Durable Natural Rubber Dog Toy** ($11.96, published, slug `kong-classic-dog-toy`, 4 verified images, supplier evidence stored) → storefront **catalog source = SUPABASE** → PDP (title/price/image/"No verified reviews yet") → admin Products → cart (add/qty/subtotal/remove). No paid order placed.
- **Quality gate:** `npm test` ✅ 88/88 · `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅ (storefront 410.45 KB / gzip 116.97 KB, admin lazy 242.37 KB) · zero console errors.
- **READY FOR PHASE 4 PRODUCT SCOUT: YES** (apply `0006` first so product writes with default/`'draft'` status succeed).

## Migration history (2026-08-17)

1. `0001_initial_schema.sql` / `0002_auth_rls.sql` / `0003_role_grants.sql` — V2 schema + customer RLS + role grants (Phase 1/3A). Not applied directly (postgres password rejected); their content is fully encoded in `0004`.
2. `0004_reconcile_live.sql` — single ordered, transactional, additive package for the SQL editor. First draft had two ordering bugs, both fixed before final apply:
   - `orders.customer_id` / `addresses.customer_id` FKs referenced `customers` before it was created → reordered (create V2 tables first, then FK columns).
   - legacy RLS policies blocked `ALTER COLUMN TYPE` on status enums ("cannot alter type of a column used in a policy definition") → POLICY CLEANUP moved to section 2, before any type change, using `pg_policies` + `to_regclass` (drops unknown legacy names like "Published products are public"). Also: dropped stale enum check constraints, guarded every legacy ALTER, reconciled `inventory` (legacy PK = variant_id), skipped the `updated_at` trigger on `creative_jobs`, wrapped everything in `BEGIN/COMMIT`.
3. `0005_drop_legacy_currency_checks.sql` — applied ✅. Dropped stale `%_currency_check` / `%_country_check` constraints (verified live: `'USD'` now accepted).
4. `0006_products_status_allow_draft.sql` — applied ✅ (draft-status product writes verified live).
5. `0007_market_intelligence_job_type.sql` — **applied ✅ (owner)** (adds `MARKET_INTELLIGENCE` to the `agent_jobs` type check; verified live — MARKET_INTELLIGENCE job rows now persist).
6. `0008_supplier_api_runs.sql` — **APPLIED ✅ (owner) + LIVE-VERIFIED (2026-08-17 REST probes)**. Creates the durable CJ point ledger: `supplier_api_runs` table with DB-enforced CHECK constraints (requested/hard budgets [50,250], `reserved_points <= hard_budget`, counters >= 0, status/provider enums — no secrets); admin-only SELECT RLS policy; and three SECURITY DEFINER RPCs: `start_supplier_api_run(p_provider, p_requested_budget)` (DB stores the clamped hard budget via LEAST(GREATEST(·,50),250)), `reserve_supplier_api_points(p_run_id, p_provider, p_action, p_is_retry)` (DB derives the cost: list=50/detail=10/freight=10 — caller can NEVER supply cost; unknown actions rejected), `finish_supplier_api_run(p_run_id, p_status)` (allowed statuses, running rows only). p_-prefixed args everywhere, EXECUTE revoked from PUBLIC, granted to authenticated ONLY, no service_role grants. Live-verified: RPCs exist + deny non-admin JWTs (400 P0001), CHECK constraints reject 9999/-5/`weird`, valid row 201, anon write blocked by RLS (real row unchanged), test rows cleaned up. **One residual-grant gap found live and fixed by 0009** (anon/authenticated still held write grants from Supabase project defaults; RLS masked them but the explicit revoke is now a migration).
7. `0009_revoke_supplier_runs_mutation.sql` — **APPLIED ✅ (owner) + LIVE-VERIFIED (2026-08-17)**. `revoke insert, update, delete on public.supplier_api_runs from anon, authenticated;` — makes the audit's "no direct counter/budget mutation" explicit at the grant level. Verified live: anon PATCH on a real row → **401 42501 "GRANT UPDATE … TO anon"** (grant gone; previously silent 204), row unchanged, cleanup done. `service_role` keeps standard ops grants (never browser-used; RPCs still refuse service-role JWTs).

## Live Supabase project (owner-provided)

- Project URL: `https://eidujmfbcfrjjleitaqp.supabase.co`
- Keys: new-style `sb_publishable_...` (browser, `apikey` ONLY) and `sb_secret_...` (server). Local `.env` (gitignored) carries `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`. Never committed.
- Admin user: `admin@luxedge.us` (`app_metadata.role='admin'`) — login, dashboard, refresh survival, logout, protected-route block all verified live.
- Live data (all real, seeded this phase): 1 category (Pet Toys), 1 supplier (KONG Company), 1 supplier_product, 1 product_candidate (approved), **1 published product (KONG Classic, USD)**, 4 product_images, 1 customers row (admin@luxedge.us).

## Catalog behavior

- `src/services/catalog.ts` `loadStorefrontCatalog()` reads published products + active categories (+ images) from Supabase; returns `null` when unconfigured/unreachable/no published products → demo catalog stays. With the KONG product published, **catalog source = SUPABASE** (verified in browser: only the KONG product renders; demo products no longer in the catalog).
- Mapped products carry `rating:0/reviews:0` → PDP shows "No verified reviews yet" (no fabricated ratings).
- Legacy schema gaps to remember when seeding via SQL/scripts: `products.title` NOT NULL (V2 writes `name` — set both); `product_images.storage_path`/`public_url` NOT NULL (V2 writes `url` — set both); `addresses.street1` etc. NOT NULL (V2 address writes need both column sets — Phase 3C work); `orders.order_number` is an identity column.

## Known issues / next owner actions

- **`0007` applied ✅** — `MARKET_INTELLIGENCE` job rows persist (verified live).
- `/api/ai/*` + `/api/fetch-page` require an admin JWT; live AI calls run against the deployed Vercel **PREVIEW** env (DeepSeek key present there, production untouched); never commit keys. Replace the temporary DeepSeek key with the owner's real key when provided.
- Checkout payment is demo mode (no processor) — explicit notice shown.
- Customer-profile sync verified live; Phase 3C should wire the admin UI (products/candidates/suppliers/orders/AI jobs/campaigns) to the real DB and reconcile legacy address/order column requirements.
- In-memory per-instance rate limiting only; shared limiter interface ready.
- 3 leftover Phase 3A debug buyer auth accounts (`buyer-debug-*`, `buyer-contract-*`) can be deleted by the owner.

## Security status

- No secrets in bundle/localStorage beyond the user's own session tokens. Bundle scan CLEAN of secret values (only env-var NAMES like `DEEPSEEK_API_KEY` appear in the admin AI-settings UI — not values; only the public publishable key is present by design).
- `.env.example` names-only; `.env` gitignored. Service-role/secret key + JWT secret are server-only. SSRF guard + timeouts + model allowlist + rate limits + admin JWT guard active.
- Admin role derives from the verified JWT claim only (`app_metadata.role='admin'`); customer insert uses the user's own token and never sets a role.
- A real postgres DB password was previously written into this file (commit `1e21290`) and is **redacted**; it remains in git history and was **not rotated** — the owner decides rotation after the project completes. Do not re-print real secret values in this file.

## Credentials genuinely required (owner)

1. ✅ Supabase project URL + publishable key + secret key — provided, wired locally.
2. Postgres DB password for direct connection — current one fails; the SQL-editor migration path (0004/0005/0006) is the working path and needs no password.
3. Optional: `SUPABASE_JWT_SECRET` (remote validation already works without it).
4. AI provider keys (server env): `DEEPSEEK_API_KEY`, `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `OPENROUTER_API_KEY`, `GEMINI_API_KEY`.
5. **Phase 4C: `CJ_API_KEY` (server env, REQUIRED for live CJ supplier discovery)** — CJ is NOT configured; the integration, proxy, UI and tests are complete and fail closed to `NOT CONFIGURED` until the owner adds this key to the Vercel env (server-side only, never `VITE_CJ_*`). Get it from the CJ Dropshipping account → API section.
6. Scraping: `SCRAPE_DO_TOKEN` (optional). Payments (later): Stripe/PayPal server keys.

## Vercel env vars (status 2026-08-17)

**PREVIEW environment (added during the live AI proof):** `VITE_SUPABASE_URL` ✅, `VITE_SUPABASE_ANON_KEY` (publishable key) ✅, `DEEPSEEK_API_KEY` ✅ (owner-supplied TEMPORARY test key — replace with the real key). **Production env: untouched** — add `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (secret key), optional `SUPABASE_JWT_SECRET`, plus AI provider keys only when the owner explicitly asks. `VITE_*` vars must be exposed to the browser build; server keys (`DEEPSEEK_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, …) are server-side only.

## PHASE 4A — PRODUCT SCOUT (CLOSED, PASS)

**What was built** — a real autonomous research pipeline (`src/features/scout/`) + an admin UI (`src/admin/ProductScout.tsx`, route `/admin/scout`, sidebar "Product Scout ⭐"). Pipeline: DISCOVER → VERIFY SOURCE → NORMALIZE → SCORE → REJECT/SHORTLIST → OWNER APPROVAL → PRODUCT DRAFT. No AI credits are consumed — extraction and scoring are deterministic rule-based; no candidate is ever auto-published.

**Engine modules** (all pure/testable): `types.ts` (VERIFIED/INFERRED/UNKNOWN evidence model), `normalize.ts` (slugify, supplier-from-domain, price/rating/review parsing, dedupe keys), `extract.ts` (rule-based page parsing: title/price/availability/shipping/origin/rating/sizes), `margin.ts` (landed cost = supplier + shipping; proposed price = 2.5× landed → .99; margin confidence LOW when shipping unknown → no auto-approve), `reject.ts` (hard filters: counterfeit/IP, medical/veterinary claims, dangerous/regulated, battery/fire, fragile/high-return, misleading claims, unavailable, no price, no images, poor USA delivery, thin margin — each with an exact recorded reason), `score.ts` (exact 100-pt weights from the spec: demand 20, supplier 15, USA delivery 15, margin 15, ratings 10, visual 10, competition 5, upsell 5, return risk 5; shortlist ≥ 75; zero points where evidence is unavailable), `persist.ts` (writes suppliers/supplier_products/product_candidates/product_scores/agent_jobs/agent_runs/agent_logs through the ADMIN JWT — RLS enforces admin-only writes; product draft honors legacy NOT NULL columns and is always `status='draft'`), `engine.ts` (runScoutResearch creates a PRODUCT_RESEARCH job, dedupes by source URL, records failures honestly).

**Admin UI** — real stat cards (row counts, no fabricated numbers), filters (status/source/score/price/margin/USA/days/category), candidates table (image, title, source, price, shipping, landed, suggested, margin %, score/100, USA, evidence status, status, rejection reason), evidence viewer modal (VERIFIED/INFERRED/UNKNOWN badges + unknown fields + risk notes + legacy-row fallback), owner actions: Approve / Reject (with exact reason) / Create Product Draft (status `draft` only). "Run Scout Run" modal with editable source URLs + live progress log.

**First live research run (one controlled test, 2026-08-17)** — 12 verified real pet-product URLs (KONG manufacturer pages + Petco/Chewy retailer pages, each pre-checked fetchable). Results persisted to the real DB: **10 candidates** (2 researchable, 7 rejected, 1 pre-existing approved), e.g. KONG Flyer **54/100** (researching), KONG Extreme **58/100** (approved by owner in test + product draft created at `status='draft'`, USD, inventory 0). Rejections were honest: 404 pages → unavailable; Chewy JS-heavy pages → no extractable price (unclear landed cost). Dedupe skipped the already-known KONG Classic. No candidates auto-published; the draft is invisible on the storefront (verified live — catalog still shows only the published KONG Classic, source=SUPABASE).

**Fixes made during the live run** — (1) `src/features/ai/importer.ts`: the Vite dev server serves `/api/fetch-page` as a **JS module** (`text/javascript` content-type containing local source), which the old HTML-only guard accepted and treated as page content. Now rejects JS/JSON content types + leading `import {` lines so local source can never be misread as a fetched page. (2) `src/features/scout/extract.ts`: strip Jina Reader `Title:` prefix. (3) `ProductScout.tsx`: legacy-evidence rendering + score join by `candidate_id`.

**Tests** — 26 new scout tests (`src/features/scout/__tests__/scout.test.ts`) covering: exact 100-pt weights, evidence-based scoring, zero-points-on-missing-evidence, margin confidence/LOW-shipping, hard-rejection filters (counterfeit/medical/dangerous/battery/unavailable), unknown-evidence handling, dedupe, supplier normalization, candidate/score/supplier persistence via a fake admin-JWT adapter, product draft (never published), approval transitions (owner-only), and RLS simulation (anon/customer writes rejected). **114/114 tests pass, tsc 0 errors, build pass** (storefront 410.46 KB / gzip 116.97 KB, admin lazy 281.55 KB — scout adds ~39 KB to the lazy admin chunk only).

**Security** — scout mutations go through the db adapter with the admin's own JWT (never service-role); RLS `owner all` policies gate suppliers/candidates/scores/jobs; anon/customer have no policies on those tables. Bundle scan CLEAN of secret values (only env-var names in the admin UI, public publishable key by design). No credentials changed.

**DB state after Phase 4A + closure** — `product_candidates`: 10 rows (1 approved legacy KONG Classic + KONG Flyer researching + KONG Extreme researching [reverted — see closure] + 7 rejected with reasons); `product_scores`: 10; `supplier_products`: 13+; `suppliers`: 14 (each real supplier ONE identity — KONG Company, Petco, Outwardhound, Chewy, Amazon, Target, Allamerican, Madeintheusamatters, Naturalpetwarehouse, Catclaws, Mickeyspetsupplies, Topdogtips, Usalovelist, Thewearify); `agent_jobs`/`agent_runs`/`agent_logs`: PRODUCT_RESEARCH + PRODUCT_SCORE + PRODUCT_QA runs recorded; `products`: KONG Classic (published) only — the KONG Extreme test **draft was deleted** (owner-gate revert).

## PHASE 4A CLOSURE (2026-08-17) — PASS

**1. TRUE AUTONOMOUS DISCOVERY** — new `src/features/scout/discover.ts`: query-based discovery (`discoverUrls`) searches public sources (DuckDuckGo HTML through the same proxy chain — no new secrets, no brittle scraping), decodes `uddg=` redirects, filters out category/search/blog/home pages, cleans tracking params, dedupes by canonical host+path. Generic category queries ("pet accessories") are **expanded into concrete pet product types** (dog toy, cat toy, dog bed, harness, grooming brush, etc. — `QUERY_EXPANSIONS`) so real product pages surface; product-specific queries are searched as-is. Manual URL mode remains as an advanced option in the Run modal. Discovery cannot bypass admin auth — it runs inside the admin-only scout UI and only fills the URL list (no DB writes by itself); writes happen only via the admin-JWT research pipeline.

**2. JOB AUDIT TRAIL — 3 distinct durable jobs** — `engine.ts` now records **PRODUCT_RESEARCH → PRODUCT_SCORE → PRODUCT_QA** as three separate `agent_jobs` (each with its own `agent_runs` + `agent_logs` rows): RESEARCH = fetch+extract+persist candidates as `researching`; SCORE = margin + hard-rejection + 100-pt score, updates status to qualified/rejected, persists score; QA = deterministic evidence QA on shortlisted candidates (images/price/availability/shipping-days/margin-confidence/unknown-fields) with PASS/FLAGGED + reasons. Every job records status, start/end, input, output summary, error, retry count. No AI provider calls; no secrets stored in job records.

**3. SUPPLIER NORMALIZATION + DEDUPE** — `ensureSupplier` now matches by **canonical domain first** (`canonicalDomain` strips `www.`, so `kongcompany.com` ≡ `www.kongcompany.com`), then slug, then case-insensitive name. Live cleanup: the duplicate `Kongcompany` row (from the Phase 4A run creating `https://kongcompany.com` vs the seeded `https://www.kongcompany.com`) was merged — its 6 `supplier_products` re-pointed to `KONG Company`, dup row deleted. Verified live: KONG, Petco, Chewy, Outward Hound each resolve to exactly one supplier identity (14 suppliers total, no dupes).

**4. OWNER APPROVAL GATE — test approval reverted** — the Phase 4A live test's KONG Extreme approve+draft was **not** owner approval, so it was reverted live: the KONG Extreme **product draft was deleted** (product + images rows), the candidate restored to its correct pre-owner state **`researching`** (score 58 < 75 shortlist threshold), all research evidence + score preserved. Enforcement is structural: `engine.ts` never calls approve/draft — statuses reach at most `qualified`; Approve / Create Product Draft exist only as explicit admin-UI buttons. (Test coverage: the engine-string tests assert no `status: 'published'` path, and the owner-only mutations test asserts anon/customer writes are rejected.)

**5. DISCOVERY LIVE TEST (one controlled run, 2026-08-17)** — query **"pet accessories"**, market **USA**, max 20 URLs. DuckDuckGo returned ~20 raw product-page URLs (after filtering category/blog/search/home pages and deduping 14 duplicates). 32 URLs (12 seed + 20 discovered) went through the full pipeline live: **17 researched, 15 failed/unverifiable** (blocked fetches: Etsy CORS, JS-heavy retailers — recorded honestly), **17 scored → 15 rejected** (category/list pages without a single-product price = "unclear landed cost", 404s = unavailable), **0 shortlisted ≥ 75** (top scores 72, 60, 52, 49, 52 — no score inflation), **QA: 0 candidates** (nothing reached shortlist). Honest result: generic queries surface mostly category/directory pages; real product pages from this run still need owner-verified sources (e.g. manufacturer URLs) — exactly why manual URL mode is kept.

**6. SECURITY** — discovery is admin-UI-only; research/scout writes go through the admin's own JWT (RLS admin-only); anon/customer have no policies on scout tables (verified in Phase 4A + tests); bundle scan CLEAN of secret values (only env-var *names* in the admin AI-settings UI; public publishable key by design); no service-role key in browser code; credentials unchanged.

**7. QUALITY** — `npm test` ✅ **125/125** (11 new tests: discovery decode/filter/dedupe/expansion/warning, canonical-domain supplier merge, QA pass/flag) · `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅ (storefront 410.46 KB / gzip 116.97 KB unchanged; admin lazy 295.79 KB / gzip 68.74 KB — discovery UI adds ~0.25 KB to the lazy admin chunk only). Console: no app errors (only expected CORS 403s from blocked research fetches, handled as `[fail]`).

**Commits:** `feat` (scout engine 3-job pipeline + discovery + supplier dedupe + UI) + `docs` (this file) pushed to `luxedge-v2`. **main untouched** (`4d0086d`).

**PHASE 4A FINAL VERDICT: PASS** — READY FOR PHASE 4B: YES (one-product autonomous listing: owner-verified sources → listing generation via secure proxy if AI keys configured → QA → owner publish).

## PHASE 4B — AI MARKET INTELLIGENCE + GUARDED AUTONOMY + ONE-PRODUCT LISTING (2026-08-17)

> **Historical build record** (state at the time; superseded by the live proof below — DeepSeek is now configured in the Preview env and the grounded call succeeded).

**What was built** — the Market Intelligence + Guarded Autonomy layer over the Phase 4A Product Scout, plus the one-product autonomous listing test (DRAFT ONLY). All AI calls go through the secure server proxy (`serverGenerate`); DeepSeek was NOT configured on this project at that time, so the **deterministic evidence layer was the live path** (the missing-key fallback is exercised and tested). No market facts are ever invented — every signal carries VERIFIED/INFERRED/UNKNOWN and unverified AI claims are dropped and recorded.

**New modules** — `src/features/scout/market.ts` (signal collector: search-pattern/competition/price-range/rating/availability signals from real discovery + researched extracts; **Market Opportunity Score /100** — demand 30, price band 20, competition 20, ratings 15, availability 15, zero points without evidence; **Final Opportunity Score** = Product 70% × product + 30% × market, documented formula; `parseMarketAnalysis` validates DeepSeek structured output and clamps ranges; `runMarketIntelligence` grounds AI claims — unsupported claims are dropped + recorded, deterministic fallback when DeepSeek is missing). `src/features/scout/autonomy.ts` (policy layer: **MANUAL / REVIEW / AUTO** modes, configurable thresholds — product score ≥ 75, market score ≥ 60, min margin 35%, max unknown fields 3, USA-delivery + QA requirements, max AI calls/run — **EMERGENCY PAUSE kill switch**, owner attention queue with why/recommended-action/risk-if-ignored/evidence; AUTO can never override hard safety gates: hard rejection, missing source cost, low margin confidence, IP/regulatory risk, QA failure, pause). `src/features/scout/listing.ts` (DeepSeek listing generation via the secure proxy — AI may rewrite ONLY verified source facts, never certifications/dimensions/materials/shipping-times/ratings/reviews/warranty/medical/inventory/sales; **factual QA** classifies every claim SUPPORTED/UNSUPPORTED/UNKNOWN, any important unsupported claim FAILS QA; **deterministic listing fallback** when AI is missing — every field traces to stored evidence). `src/features/scout/aiCost.ts` (evidence fingerprint + in-memory cache so identical evidence never pays twice, deterministic prefilter before any AI spend, per-run AI-call budget). `engine.ts` gains `runMarketIntelligenceJob` (records a durable MARKET_INTELLIGENCE job → signals → market score → analysis). Migration `0007` adds `MARKET_INTELLIGENCE` to the `agent_jobs` type constraint.

**Admin UI (Product Scout page)** — "Market Intelligence" modal (query + market + run; shows signals count, market score/100, AI vs deterministic, trend/competition/price-band/risks/unsupported-claims/recommended searches); **Autonomy Policy** panel (MANUAL/REVIEW/AUTO toggle, thresholds, save, Emergency Pause button); **Owner Attention Queue** (Scan candidates → routes exceptions like hard-rejection, missing market score, high-margin risk, QA flags; resolve); per-candidate **Prepare Listing** (Brain) button → generates listing (AI or deterministic) → factual QA → AUTO policy decision → creates the product DRAFT (never published).

**One-product test (live, 2026-08-17)** — Market Intelligence compared the viable candidates for "dog toys / USA": 12 real product URLs discovered, **Market Opportunity Score 28/100** (deterministic — honest: 9 competing domains, no price/rating signals folded in this standalone run). Final Opportunity: **KONG Extreme = 0.7×58 + 0.3×28 = 49** vs KONG Flyer = 46 → **KONG Extreme selected on evidence** (highest product score, not because it pre-exists). Listing prepared via the UI: **deterministic listing** (AI not configured), **factual QA PASS**, **AUTO POLICY DECISION: eligible_for_auto_publish = NO** (product 58 < 75 threshold), **ACTUAL ACTION: KEEP DRAFT** — product draft `Luxedge KONG Extreme` created at `status='draft'`, `currency='USD'`, inventory 0, never published (storefront verified: only the published KONG Classic renders). The durable MARKET_INTELLIGENCE job row could not be written live because `0007` is not yet applied (23514 constraint error surfaced cleanly in the UI) — apply 0007 to enable job persistence.

**Tests** — 20 new Phase 4B tests (market signal normalization, market opportunity scoring incl. zero-evidence, final-opportunity 70/30 formula, AI structured-output validation + clamping, evidence grounding / unsupported-claim drop, DeepSeek missing-key fallback, autonomy MANUAL/REVIEW/AUTO, emergency pause, owner-attention routing + dedupe, AI-cache fingerprint, deterministic prefilter + budget, listing factual QA incl. hallucination rejection, deterministic fallback, never-auto-publish, MARKET_INTELLIGENCE job persistence). **146/146 tests pass · tsc 0 errors · build pass** (storefront 410.46 KB unchanged; admin lazy 335.53 KB — includes the Phase 4B panels).

**Security** — DeepSeek key server-side only (client uses `serverGenerate` → `/api/ai/generate`, admin JWT); no service-role key in the browser bundle (bundle scan CLEAN of secret values — only env-var *names* like `DEEPSEEK_API_KEY` in the admin AI-settings UI, public publishable key by design); scout/autonomy mutations remain admin-JWT-only; owner attention + autonomy config are local-state UI (no secret material); credentials unchanged.

**Known blockers** — (1) **Apply `0007_market_intelligence_job_type.sql`** in Dashboard → SQL Editor to persist MARKET_INTELLIGENCE jobs (the deterministic analysis already runs; only the durable job record is blocked). (2) DeepSeek (or another AI provider) key is not configured — Market Intelligence shows "not configured" and listing generation uses the deterministic fallback; add a server-side key when AI-generated copy is wanted.

## PHASE 4B AMENDMENT — FLEXIBLE AI CONTROL (2026-08-17)

**What was built** — the owner now decides how much control AI gets at any moment. Luxedge runs as **100% HUMAN CONTROL**, **HUMAN + AI COPILOT**, or **GUARDED AUTONOMOUS** without changing the underlying architecture. All provider calls remain SERVER-SIDE ONLY through the existing `/api/ai/generate` proxy (admin JWT, keys in env vars). No key ever reaches the browser.

**New modules** — `src/features/scout/aiControl.ts` (the control plane): **AI SERVICES master ON/OFF** (OFF = ZERO AI provider calls anywhere; deterministic scout, DB and storefront keep working); **CONTROL MODE MANUAL / ASSISTED / AUTONOMOUS** (MANUAL = AI acts only on explicit owner request; ASSISTED = AI researches/analyzes/recommends/drafts/QAs but important transitions wait for the owner; AUTONOMOUS = AI may advance allowed low-risk steps per policy — hard gates/budgets/pause always active); shared **EMERGENCY PAUSE** kill switch; **COST STRATEGY ECONOMY / BALANCED / QUALITY** (ECONOMY = cheapest capable provider, BALANCED = routing matrix, QUALITY = premium model for important decisions); **SECOND OPINION OFF / IMPORTANT ONLY / ALWAYS** (extra provider verifies important decisions; disagreement ≥ 10 score points is flagged for the owner); per-provider **ON/OFF switches + task allowlist + cost rank + daily call limit** (turning a provider OFF blocks ALL calls to it — even as a fallback — without deleting its configuration); **feature-level switches** (Market Intelligence, Discovery, Analysis, Listing Writer, Listing QA, Pricing, Creative, Optimization); configurable **task → primary → fallback routing matrix**; **TAKE MANUAL CONTROL / RESUME AI CONTROL** (stops new autonomous actions, preserves all state/evidence, drops autonomy policy to MANUAL / restores REVIEW or AUTO); and the **AI Router Decision Log** (every invocation records task/provider/model/why/fallback/fingerprint/time/result/control mode — never prompts, outputs, or key material; key-like strings are scrubbed before persisting). `src/features/scout/aiRouter.ts` (single funnel enforcing master → feature → mode → pause → routing → capped retries (max 2 attempts) + backoff → fallback (respects provider ON/OFF) → second opinion → decision log; throws catchable errors so callers fall back to deterministic paths — AI OFF never breaks Luxedge). `market.ts` and `listing.ts` now route their AI calls through the router; deterministic fallbacks are unchanged and verified.

**Admin UI** — new **AI Control Center** page (`/admin/ai-control`, sidebar "AI Control ⭐"): status banner, master switches (AI Services / Emergency Pause), control-mode / cost-strategy / second-opinion selectors, TAKE MANUAL CONTROL + RESUME AI buttons, providers table (ON/OFF, API configured, health online/error/not-configured, model, cost rank, calls + failures today — all real numbers from the server status + decision log), 8 feature switches, full task-routing matrix, and the owner attention queue (exceptions only, with resolve). The Product Scout header now shows a live control strip (AI Services / Control Mode / pause) with a link to the Control Center. Default safe configuration: **AI SERVICES ON · CONTROL MODE ASSISTED · PAUSE OFF · BALANCED · SECOND OPINION IMPORTANT_ONLY · DeepSeek ON (if configured), Claude/OpenAI/OpenRouter/Gemini OFF**.

**Verified live (browser, admin session)** — Control Center renders with the safe defaults; toggling AI Services OFF flips the banner to "AI Services: OFF — Manual operation active" and persists; TAKE MANUAL CONTROL sets control mode MANUAL + autonomy policy MANUAL (evidence/config preserved); settings restored to the safe defaults afterward. Product Scout renders the new control strip with the real DB candidates/jobs. No console errors.

**Tests** — 31 new amendment tests (master OFF = zero provider calls; MANUAL blocks implicit / allows explicit AI; ASSISTED + AUTONOMOUS routing; DeepSeek/Claude/OpenAI OFF = zero calls to that provider incl. fallback; enabled+configured fallback on primary failure; fallback-disabled behavior with capped retries; second-opinion IMPORTANT_ONLY/ALWAYS/OFF; provider disagreement flag; ECONOMY/BALANCED/QUALITY routing incl. premium model; feature-specific OFF; emergency pause; TAKE MANUAL CONTROL / RESUME handoffs incl. autonomy-policy sync; provider failure stays deterministic; decision log never logs prompts/secrets + scrub of key-like strings; callsToday/providerHealth from the log). **177/177 tests pass · tsc 0 errors · build pass** (storefront 410.46 KB unchanged; admin lazy 358.48 KB — includes the Control Center page).

**Security** — master/feature/provider switches are admin-UI-only (route behind `ProtectedRoute requireAdmin`); no service-role key in the bundle (scan CLEAN of secret values — only env-var *names* in the admin UI); decision log contains no prompts/outputs/secrets (scrubbed); provider calls go through the admin-JWT server proxy; autonomy limits unchanged (AI can never change credentials, RLS, budgets beyond config, pay suppliers, spend ad money, bypass hard rejection, disable pause, or promote users); credentials unchanged.

**Known blockers (updated)** — (1) `0007` applied ✅ — MARKET_INTELLIGENCE jobs persist (verified live). (2) **DeepSeek configured ✅ in the Vercel PREVIEW env only** (owner-supplied TEMPORARY test key) and the grounded live Market Intelligence call succeeded — see "PHASE 4B FINAL — LIVE AI PROOF" below. (3) The local Vite dev server serves `/api/*` as transformed source, so `/api/ai/*` runs via the deployed preview; the dev-only proxy in `vite.config.ts` (env-gated) routes local admin UI calls to the deployed functions. (4) Direct postgres password still rejected — SQL-editor/REST path is the working path.

**Phase 4B verdicts (distinguished, per owner audit)** —
- **AI CONTROL ARCHITECTURE = PASS** — master AI Services switch, MANUAL/ASSISTED/AUTONOMOUS control modes, per-provider ON/OFF + task routing + fallback, cost strategies, second opinion, feature switches, TAKE MANUAL CONTROL / RESUME AI CONTROL, decision log, and the AI Control Center UI are implemented, tested (178/178) and browser-verified. These are configuration/architecture — they do NOT depend on a live provider.
- **DETERMINISTIC PIPELINE = PASS** — discovery → research → scoring → rejection → QA → supplier dedupe → one-product DRAFT-only listing all work live against the real DB with zero AI calls.
- **LIVE AI-GROUNDED MARKET INTELLIGENCE = PASS** — all three prerequisites completed and verified live against the REAL deployed backend: (a) `0007` applied ✅; (b) **DeepSeek configured ✅ in the Vercel PREVIEW env** (temporary test key, server-side only, health ONLINE via the app's own test endpoint); (c) **one grounded Market Intelligence call succeeded** against real collected evidence (12 URLs / 8 domains, opportunity score 50/100, honest unknowns, durable job row with provider/model).

## PHASE 4B FINAL — LIVE AI PROOF (2026-08-17) — PASS

**Deployment wiring (this proof required a REAL serverless backend):**
- Vercel project `luxedge-website` (team `8002salman-2281`) linked; **PREVIEW environment only** now has `DEEPSEEK_API_KEY` (owner-supplied TEMPORARY test key), `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`. **Production env unchanged** (no `DEEPSEEK_API_KEY` in Production). Key value never printed/committed/stored anywhere except Vercel's env store.
- **Bug found & fixed — deployed `/api/*` 500'd on Vercel:** the ESM bundle did `import '../_lib/providers'` without `.js` extension → `ERR_MODULE_NOT_FOUND` on Vercel's native ESM loader (local `vercel dev` tolerated it). Fixed by adding explicit `.js` extensions to every relative import in `api/*` (source, tests). Redeployed preview → functions work (401 without auth, 200 root via the protection-bypass header).
- `vite.config.ts` gained an **env-gated dev-only proxy**: when `VERCEL_PREVIEW_API` (+ optional `VERCEL_PREVIEW_BYPASS`) are set in the gitignored `.env`, local Vite proxies `/api/*` to the deployed preview so the local admin UI exercises the REAL serverless functions (auth guard, AI router, provider adapters) instead of Vite serving `/api` as source. `.gitignore` += `.vercel` and `.env*` (`.env.example` stays tracked/unchanged).
- Direct postgres connection (owner-provided URL): **password still rejected** — no guessing; REST path with the owner's secret key works and is what the app uses.

**Real provider health:** `/api/ai/status` on the deployed preview (admin JWT): **DeepSeek configured = true**. Smallest safe health call through the app's own test endpoint: **"Connected successfully!"** — DeepSeek ONLINE (model: deepseek-chat). Admin auth, AI router, and provider adapter all exercised through the REAL application architecture (no direct curl-to-DeepSeek bypass).

**One grounded Market Intelligence run (live, deployed preview):** query **"pet accessories"**, market **USA**. 12 real product URLs discovered → real signals collected (12 URLs, 8 distinct domains) → deterministic pre-score **28/100** → **DeepSeek grounded analysis succeeded**: output explicitly referenced the real evidence (12 URLs, 8 domains) and honestly recorded unknowns ("no volume or growth data" = UNKNOWN, not invented) → **Market Opportunity Score 50/100**. `MARKET_INTELLIGENCE` job row persisted to the live DB (**aiUsed=true, marketScore 50, provider/model recorded** — `completeJob`/engine extended so provider+model land on the durable job record).

**One-product test (evidence-selected, DRAFT ONLY):** surveyed ALL live candidates by stored evidence — strongest was **Natural Pet Warehouse "Quality Dog Toys Made in USA" (Product Score 69/100)**, above KONG Extreme (58) — selected on evidence, not because it pre-exists. DeepSeek listing generated through the router → **factual listing QA PASS** (every material claim classed SUPPORTED/UNKNOWN; no unsupported claim survived) → product draft persisted: `status='draft'`, `currency='USD'`, price 49, title "Luxedge Premium Quality Dog Toys – Made in USA". **AUTO POLICY DECISION: eligible_for_auto_publish = NO** (69 < 75 threshold) → **ACTUAL ACTION: KEEP DRAFT**. Not published (storefront still shows only the published KONG Classic; catalog source = SUPABASE). Added durable **LISTING_GENERATE** job persistence in `prepareListing` (constraint-compatible).

**Real AI control tests (live, deployed preview):**
- **A) AI SERVICES = OFF** → Market Intelligence run completed deterministically with **zero DeepSeek invocations** (decision log flat; run log "DeepSeek unavailable… deterministic analysis").
- **C) DeepSeek provider = OFF** → **zero DeepSeek calls** (decision log unchanged).
- **D) EMERGENCY PAUSE = ON** (with DeepSeek available) → **found + fixed a real bug**: the Control Center's pause toggle wrote only `luxedge_ai_control.emergencyPause` while the runtime gate reads the canonical `luxedge_scout_emergency_pause` key (`isEmergencyPaused()`) — so pause from the Control Center never engaged. Fixed `AiControlCenter.tsx` to sync the canonical key; added a regression test; **verified live** — pause now engages the canonical key and shows "EMERGENCY PAUSE ACTIVE". Truthful pause semantics (documented here, no behavior change): while paused the AI gate denies every AI provider call and every autonomous mutation; only deterministic read-only research/analysis continues, because it makes no AI calls and performs no mutations. (Earlier wording cited "Master Plan §14" — §14 is actually STOREFRONT/BRAND; corrected to describe the implemented behavior, not a section reference.)
- Safe defaults restored: **AI SERVICES = ON · CONTROL MODE = ASSISTED · DeepSeek = ON · EMERGENCY PAUSE = OFF**. No production auto-publish enabled.

**Security scans (values never printed):** server-side-only `DEEPSEEK_API_KEY` (no `VITE_DEEPSEEK_*` anywhere); bundle scan CLEAN of secret values; `git diff` CLEAN of the key; decision logs/DB audit records contain no key/authorization material (scrub verified); direct postgres password still unverified — no guessing, credentials unchanged.

**Quality:** `npm test` ✅ **178/178** (incl. the new pause-key regression test) · `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅.

**AI cost/audit:** 2 real DeepSeek calls total this proof (1 Market Intelligence + 1 listing generation) — deterministic prefilter ran first, evidence-fingerprint cache in place, no duplicate calls for unchanged evidence, 0 retries. Decision log rows record task/provider/model/control mode/fallback/fingerprint/result summary/timestamp; token/cost metadata not yet returned by the server generate endpoint (recorded honestly).

**Verdicts now:** AI CONTROL ARCHITECTURE = PASS · DETERMINISTIC PIPELINE = PASS · **LIVE AI-GROUNDED MARKET INTELLIGENCE = PASS** (0007 applied ✅ + DeepSeek configured in Preview ✅ + grounded call succeeded ✅) · **QUALIFIED BUSINESS WINNER = NONE FROM SEARCH** (no candidate ≥75 product / ≥60 market — see BUSINESS-GATE CLOSURE below). PHASE 4B FINAL = **PASS** (architecture + proof), with the business selection gate left open pending a genuine winner.

## PHASE 4B BUSINESS-GATE CLOSURE (2026-08-17)

**Owner audit accepted the DeepSeek proof but flagged the business gate:** the 69/50/63 candidate was NOT a qualified winner (Product <75, AUTO eligible NO). Closure actions taken:

**1. Sub-threshold test drafts removed (live DB, owner-gate revert).** Removed BOTH test-created sub-threshold product drafts (never owner-approved):
- `Luxedge Premium Quality Dog Toys – Made in USA` (candidate 69/100) — the draft named in the audit.
- `Luxedge KONG Extreme` (candidate 58/100) — same class: Phase 4B one-product test artifact, also sub-threshold.
Deleted their 50 draft-only `product_images` rows + the two `products` rows. **Preserved:** candidates (restored to `researching`), evidence, Product/Market scores, AI analysis, all MARKET_INTELLIGENCE/PRODUCT_RESEARCH/SCORE/QA jobs, runs, logs. Published **KONG Classic untouched** (verified — it is now the ONLY product row). Note: no durable `LISTING_GENERATE` job row had actually been persisted in the DB (that persistence landed after the live run) — nothing to delete, reported honestly.

**2. State corrected:** REAL AI-GROUNDED MARKET INTELLIGENCE = PASS (proven), but **QUALIFIED BUSINESS WINNER = NONE FROM FIRST RUN** — the 69/50/63 candidate is recorded as researching, NOT a winner. READY FOR HIGGSFIELD = NO until a genuine ≥75/≥60 winner exists.

**3. Real winner search (live, DeepSeek grounded):** one additional MARKET_INTELLIGENCE run for **"pet enrichment" / USA** succeeded through the real router — DeepSeek analysis grounded on real evidence (12 URLs, 8 domains), market score **55/100**, honest unknowns ("evidence does not quantify volume or growth"), job `54c6e878` persisted (ai=true). DeepSeek recommended searches: *interactive dog toys, puzzle feeders for dogs, cat enrichment toys*. Two full deterministic research passes ran those + "pet enrichment toys": **57 candidate URLs discovered → 24 researched (33+52 fetch-failed honestly: Etsy/Chewy/Petco JS-heavy or rate-limited) → 19 rejected → 0 shortlisted ≥75** (top scores 72/70/69/66/61/58 — no inflation). Final counts: 51 candidates (41 rejected, 9 researching, 1 approved).

**Exact bottleneck (honest):** generic query discovery (DuckDuckGo HTML) surfaces category/directory/list pages and Amazon *search* URLs, not single-product supplier pages — so `unclear landed cost` (no extractable product price), `unavailable`, or `unusable images` reject them. The strongest REAL researchable candidates (Natural Pet Warehouse 69, PetSmart cat toys 61, KONG Extreme 58, KONG Flyer 54) all fall below 75. To reach a qualified winner the search needs owner-verified manufacturer/retailer product URLs (manual mode) or a richer discovery source that returns single-product pages with price + USA shipping evidence.

**4. Emergency-pause §14 misreference fixed (docs only, no behavior change):** code comment in `AiControlCenter.tsx` and the state doc cited "Master Plan §14" for "read-only analysis may continue" — §14 is actually STOREFRONT/BRAND. Corrected both to describe the implemented behavior truthfully: while paused the AI gate denies every AI provider call and every autonomous mutation; only deterministic read-only research continues (it makes no AI calls and performs no mutations).

**Quality:** `npm test` ✅ **178/178** · `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅. **Security:** no new secrets, no key material in git/logs/DB, credentials unchanged, KONG Classic live. **GIT:** branch `luxedge-v2`; docs + doc-comment commits pushed; **main untouched** (`4d0086d`).

## PHASE 4C PRE-LIVE HARDENING (2026-08-17) — CJ ECONOMICS + VARIANT + FREIGHT + POINT BUDGET

**Owner audit accepted the CJ architecture but required business-critical fixes BEFORE spending CJ API points. All applied (fixtures-only; no CJ key used):**

**1. Enrichment before final scoring.** `runSupplierSearch` is now a controlled flow: CJ `listV2` (ONE call, no pagination) → **Stage A free** deterministic prefilter + `rankCjRecord` (US inventory signal, verified warehouse, price, image, delivery cycle, listing count — listV2 data only) → **Stage B paid** `product/query` for the top ranked set → one internally-consistent selected variant → `freightCalculate` for the strongest subset with verified landed cost → Product Score → Market Score → QA. Never freight-quotes all 30–50 results.

**2. Hard per-run CJ point budget.** New `src/features/suppliers/cj/points.ts`: `CJ_POINT_COST` (listV2=50, productQuery=10, freightCalculate=10), `CJ_POINTS_BUDGET_PER_RUN = 250`, `CjPointBudget` tracker (canSpend/spend per action, usage: budget/used/remaining/list/detail/freight/retries/denied), `enrichmentCaps` (250 → 1 search 50 + 12 detail 120 + 6 freight 60 = 230 ≤ 250). Budget is a deterministic business control — **AI can never raise it** (only the owner may pass a different `pointsBudget`). Usage persisted in the job output.

**3. Selected-variant consistency.** `SupplierProductRecord.selectedVariant: SupplierVariantEvidence | null` — THE SAME variant drives variantId, variant SKU, variant price, weight, dimensions, US inventory, verifiedWarehouse, the freight quote and the landed cost. `selectVariant()` priority: verified US inventory → available US inventory → viable cost → any valid variant. No internally consistent variant → **variant UNKNOWN, no freight, no high-confidence margin**. Tested: variant[0] no US stock, variant[1] verified US stock → variant[1] used consistently.

**4. Dynamic freight origin.** The old hardcoded `startCountryCode = 'CN'` is gone. Origin comes from the selected variant's inventory/warehouse evidence (`originCountry`); multiple origins resolve deterministically (verified-stock country first) and the choice is recorded. Unknown origin → freight UNKNOWN, never fabricated.

**5. Total shipping cost.** `normalizeCjFreight` prefers **totalPostageFee**; falls back to logisticPrice ONLY when the total is genuinely absent and records the LIMITATION; preserves taxesFee / clearanceOperationFee / tariff / carrier / arrival range / origin / destination / observed_at.

**6. Exact delivery-cycle parsing.** `parseDeliveryCycle`: "3-5" → {3,5}, "5" → {5,5}, invalid → UNKNOWN. Never improves supplier evidence (the old code turned "3-5" into min 1).

**7. No arbitrary price hard rejects.** Removed the universal <$1 and default >$40 prefilter rejects. Only price ≤ 0 (invalid/unverifiable) hard-rejects. Optional min/max supplier-cost filters apply ONLY when explicitly supplied for a search strategy. Regression-tested: $0.80 accessory and $45 premium product both pass the default prefilter.

**8. Manufacturer inflation fixed.** CJ official-API records are a VERIFIED supplier-platform source — `sourceIsManufacturer` is now **false** unless manufacturer status is independently verified; CJ records get the verified-supplier (10) not manufacturer (15) reliability points. Regression test added.

**9. Score honesty audit.** `score.ts` no longer gives free points for missing evidence: competition "neutral +3" → 0 ("no competition evidence"), upsell "neutral +1" → 0 ("no variant/upsell evidence"), return-risk +5 now requires the risk dimensions were actually assessed (risk flags are evidence-derived; an unassessable candidate gets 0). Weights unchanged (100), threshold unchanged (75). Existing historical score tests re-run and still pass (fully-evidenced candidates retain their points via sizes/ratings/margin; evidence-less candidates score lower).

**10. Point-aware retry policy.** `api/_lib/cj.ts` `cjFetch` now distinguishes **paid** (listV2/query/freight: max 1 retry) vs **auth/network** endpoints (max 2); hard 4xx / validation / auth-config errors are NEVER retried; 429 respects backoff but stays within the run budget; retry reason recorded. Fixed a retry-loop bug found by tests (a hard 4xx used to trigger a second fetch).

**11. Market score stays separate.** `listedNum` is recorded with its truthful meaning (platform listing count) — never renamed to sales/orders/demand/reviews.

**12. Live-readiness simulation (fixtures, NO key).** Tests simulate 50 listV2 records → prefilter → 10 detail calls → 5 freight calls = 200/250 pts within budget (one search call, no pagination, zero denials, enrichment capped ≤12); and a no-winner run reports shortlisted **0** honestly (zero candidates persisted).

**Security:** CJ_API_KEY still server-side only, never added, no `VITE_CJ_*`, admin-JWT proxy, secret scrub, no order/payment/sourcing endpoints. **READY TO ADD CJ_API_KEY: YES** (integration is fail-closed NOT CONFIGURED until then). **READY FOR LIVE CJ WINNER SEARCH: YES only after key.**

## PHASE 4C FINAL PRE-KEY AUDIT (2026-08-17) — THREE GAPS FIXED BEFORE ANY CJ POINTS

**Independent owner audit found three correctness gaps in the hardening pass. All fixed (fixtures-only, NO CJ key, zero live CJ calls):**

**1. Point budget is now a REAL hard cap, enforced SERVER-side.** The old flow checked `canSpend()` client-side then spent AFTER success — a paid retry inside the server client could exceed the run budget and failed paid calls were recorded as 0. Now `CjServerRunBudget` (server-scoped per-run token in `api/_lib/cj.ts`) **reserves the endpoint point cost BEFORE every actual outbound paid request — including every paid retry**. If remaining budget is insufficient the HTTP request is NEVER issued; the proxy returns the safe `CJ_POINT_BUDGET_EXHAUSTED` signal (no outbound call). A run-scoped map threads one budget through search → detail → freight; the proxy's `budget` action exposes the authoritative usage.

**2. The hard cap cannot be raised by callers/AI.** `CjPointBudget` and `CjServerRunBudget` clamp ANY requested budget to `CJ_POINTS_HARD_MAX = 250` (requested 100 → 100, 250 → 250, 500/1000/NaN → 250). Client-side `pointsBudget` remains UX/forecast only; server accounting is authoritative. Client "owner" flags are never trusted — a separate authenticated server-side owner policy would be required for a real override (not built, not needed now).

**3. True point usage persisted.** The RESEARCH job output now records server-authoritative usage — `budget / actual_reserved_points / list attempts / detail attempts / freight attempts / paid retries / denied / remaining` — labeled honestly as **estimated/authorized CJ point consumption** (every outbound paid request incl. retries represented; CJ's exact supplier billing is not claimed unless CJ itself reports it). No secrets in any record.

**4. SKU → size score inflation fixed.** `evidenceFromSupplierRecord` no longer maps `sizes = [selectedVariant.sku]`. A SKU/variant id is NOT size evidence, variant-upsell evidence or competition evidence. `sizes` stay UNKNOWN unless real variant-option attributes (size/color/pack/model) demonstrate customer-selectable options. SKU + variant id remain in the supplier raw/variant evidence only. Regression test: one SKU with no option semantics → **zero** size-derived upsell/competition points. Consequence (truthful, per audit §9): a CJ-only candidate — no customer ratings, no variant-option semantics — has an honest ceiling of exactly **75/100** (demand 20 + supplier 10 + delivery 15 + margin 15 + visual 10 + competition 2 keyword + return-risk 3 = 75); nothing is compensated elsewhere to restore old scores.

**5. Market business gate enforced (scores stay separate).** The supplier search now accepts a grounded `marketContext` (MI job id / opportunity / Market Score / evidence fingerprint / observed_at) and the run is linked to the exact MI evidence that generated the search query. Two qualification levels, never conflated:
- **PRODUCT_SHORTLISTED** = Product Score ≥75 + no hard rejection.
- **BUSINESS_QUALIFIED** = Product Score ≥75 + **Market Score ≥60** + QA PASS + verified landed cost (margin HIGH) + sufficient USA delivery evidence + no hard rejection. Only BUSINESS_QUALIFIED may enter "QUALIFIED WINNER READY FOR OWNER REVIEW".
Market Score is NEVER invented: no valid linked MI context ⇒ **Market Score = UNKNOWN (null)** ⇒ BUSINESS_QUALIFIED = false; it is never defaulted to 50/60/any neutral number and never recalculated from CJ `listedNum`. The market link (market_intelligence_job_id / market_score / market_evidence_fingerprint / market_observed_at / supplier_search_query) is persisted on the RESEARCH job output so every run can prove WHY a product was searched and WHICH market evidence justified it.

**6. Winner result model.** `SupplierSearchRunResult` reports `productShortlisted`, `qaPassed` and `businessQualified` separately, with per-candidate `productScore / marketScore / finalOpportunityScore / landedCostConfidence / usaDeliveryConfidence`. Zero qualified ⇒ **BUSINESS QUALIFIED WINNER = NONE** reported honestly.

**Live-readiness simulation (fixtures, NO key) — all green:**
- **Server budget:** budget 250 — 1 list (50) + 10 detail (10 each) + 5 freight (10 each) incl. one paid retry = every outbound attempt reserves points; authorized spend can never exceed 250. An attempted call with only 5 points remaining issues **NO outbound HTTP request** (denied counted). Requested client budget 1000 ⇒ server hard cap stays 250 (clamped).
- **Market gate:** Product 75 / Market 55 + QA PASS ⇒ PRODUCT_SHORTLISTED YES, BUSINESS_QUALIFIED NO. Product 75 / Market 65 + QA PASS + landed cost HIGH + USA evidence ⇒ BUSINESS_QUALIFIED YES (Final = 0.7×75 + 0.3×65 = 72). Product 75 / Market UNKNOWN ⇒ BUSINESS_QUALIFIED NO.
- **SKU honesty:** one SKU, no real size/options ⇒ no size/upsell bonus (competition ≤2 keyword-only, upsell 0).

**Quality:** `npm test` ✅ **239/239** (10 new audit tests: server-budget clamp/reserve-before-call/denied-no-call/exhausted signal, retries counted, market gate 55/65/UNKNOWN, market link persisted, server usage surfaced, SKU-honesty no-size-bonus) · `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅. **Security:** CJ key NOT added, live CJ calls = 0, no browser secrets, credentials unchanged. **GIT:** pushed to `luxedge-v2` only; **main untouched** (`4d0086d`).

**CJ INTEGRATION ARCHITECTURE = PASS · PRE-LIVE BUSINESS GATES = PASS · LIVE CJ SUPPLIER PROOF = PENDING · READY TO ADD CJ_API_KEY = YES.**

## PHASE 4C — AUTONOMOUS SUPPLIER DISCOVERY (CJ OFFICIAL API FIRST) (2026-08-17)

**Why:** the Phase 4B bottleneck was discovery source quality — DuckDuckGo generic discovery returns category/directory/search pages, not single-product supplier records, causing `unclear landed cost`/`unavailable`/`unusable images` rejections. Phase 4C adds an official supplier API as the primary discovery source so the AI hypotheses feed REAL product records (real price, US inventory, variants, freight) into the existing scout pipeline.

**What was built (provider-neutral, READ/RESEARCH only):**
- `src/features/suppliers/types.ts` — `SupplierDiscoveryAdapter` interface (searchProducts / getProduct / getShippingEvidence / healthCheck) + `SupplierProductRecord` with honest VERIFIED/INFERRED/UNKNOWN fields. **US inventory verified ≠ USA delivery verified** — they are distinct fields and never conflated. Future suppliers implement the same interface without touching the scout.
- `src/features/suppliers/cj/` — `normalize.ts` (official CJ V2 DTOs `product/listV2` + `product/query` + `logistic/freightCalculate` → `SupplierProductRecord`; price/inventory/images/category/delivery-cycle mapped only when CJ returned them, else UNKNOWN; `normalizeCountryCode` USA→US; **selected-variant model + exact delivery-cycle parsing + totalPostageFee-aware freight normalization** — see hardening section), `dedupe.ts` (stable keys: `cj:{pid}` / `cj:{pid}:{vid}` / `cj:sku:{sku}` — pid/SKU authoritative, URL fallback only), `prefilter.ts` (deterministic, BEFORE any AI: non-pet drift, counterfeit/IP risk, medical/veterinary claims, battery/fire risk, invalid price ≤0, unusable images, zero/unknown US inventory when targeting US — each with an exact recorded reason; optional explicit min/max cost only; **no universal <$1 / $40 hard rejects**), `points.ts` (CJ point budget — see hardening), `health.ts` (raw→typed health mapping, never trusts server text), `adapter.ts` (browser adapter that calls ONLY the Luxedge server proxy with the admin JWT — CJ credentials never touch the browser).
- `api/_lib/cj.ts` — **server-side CJ client** (runs only inside `/api` functions): reads `CJ_API_KEY` from env; access-token auth + **refresh-token flow** (`getAccessToken` → cache → `refreshAccessToken` on expiry, fallback to full auth); timeouts (15s), capped retries (max 2) + backoff on 429/5xx; **secret scrubbing** (`cjSafeError` — CJ tokens/keys replaced with `***` before any error reaches the client). Endpoints implemented against the current official docs (`developers.cjdropshipping.cn`): `product/listV2` (keyword/country/price/verifiedWarehouse filters), `product/query` (variants + per-country inventory), `globalWarehouseList`, `logistic/freightCalculate` (real USA freight cost + arrival days). **No order/payment/sourcing endpoints exist** — READ/RESEARCH only.
- `api/suppliers/cj.ts` — protected serverless proxy (admin JWT via `requireAdmin`, rate-limited, actions: health / search / product / freight). Returns normalized records only; errors scrubbed. Browser never calls CJ directly.
- `src/features/scout/supplierSearch.ts` — `runSupplierSearch`: CJ records → dedupe → deterministic prefilter → persist candidates → **reuses the SAME 3-job audit trail** (PRODUCT_RESEARCH → PRODUCT_SCORE → PRODUCT_QA) as URL research, so the audit model is uniform. Never approves/drafts/publishes. Supplier discovery is a **data source, not an AI provider** — AI OFF still allows an explicit owner-run CJ search (zero AI calls). Margin confidence stays LOW until freight is quoted (shipping UNKNOWN is never silently estimated).
- `src/admin/ProductScout.tsx` — compact additions: **CJ Supplier API health chip** in the control strip (NOT CONFIGURED / OFFLINE / RATE LIMITED / ONLINE, from a real server probe) and an **"Official Supplier Search (CJ)"** section in the Run modal (query + max records + live progress log). No giant dashboard.
- `.env.example` — added `CJ_API_KEY` (names-only, server-side).

**Tests (31 new, `src/features/suppliers/__tests__/cj.test.ts`):** normalization (listV2 + detail, price/image/inventory only when returned, null record when pid/title missing, US-inventory verified vs total vs in-country distinction, country-code normalization), stable pid/SKU dedupe across repeated searches, prefilter (non-pet/IP/medical/battery/missing-price/impossible-margin/missing-stock/zero-stock/unusable-images, batch reasons), evidence honesty (shipping cost UNKNOWN + margin LOW until freight, rating never invented, shippingDays verified only when CJ provides a cycle), `calculateMargin` confidence, Product Scout integration (`runSupplierSearch` persists the RESEARCH→SCORE→QA trail with admin JWT; fails closed on non-admin token; **threshold never lowered — `SHORTLIST_THRESHOLD === 75`**), secret isolation (`cjSafeError` scrubs tokens/keys; health mapping rejects arbitrary text), token refresh (server-side, cached within TTL, no repeated auth), API failure + rate-limit retry with caps, and **no order/payment calls**.

**Quality:** `npm test` ✅ **209/209** (31 new) · `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅ (storefront 410.46 KB unchanged; admin lazy grows by the scout/supplier UI only). **Security:** CJ key server-side only (never `VITE_CJ_*`, never in the browser adapter — it calls the admin-JWT proxy); no new secrets anywhere; credentials unchanged; proxy is admin-guarded + rate-limited + scrubbed.

**Controlled CJ search: NOT RUN (correct per spec §16 — CJ credentials are absent).** The integration is complete and fail-closed: with no `CJ_API_KEY`, `/api/suppliers/cj` returns `health: not_configured`, the UI chip shows **CJ Supplier API: NOT CONFIGURED**, and searches return zero records with a clear warning. **Owner action required:** add `CJ_API_KEY` to the Vercel env (server-side; Preview for testing), then a single controlled search (DeepSeek's grounded hypotheses → 30–50 CJ records → US-inventory prefer → normalize/dedupe/prefilter/research/score/QA) can run — with thresholds unchanged (Product ≥75 / Market ≥60, QA PASS, no hard rejection, WINNER=NONE reported honestly if nothing qualifies).

## PHASE 4C FINAL LIVE-READINESS (2026-08-17) — DURABLE POINT LEDGER + VERIFIED MARKET PROVENANCE

**Owner audit found two live-readiness blockers before real CJ points are spent. Both fixed (fixtures only, NO key, zero live CJ calls):**

**1. CJ point budget is now a DURABLE atomic ledger in Supabase — NOT process memory.** The old per-warm-instance `Map` is no longer the authority (it remains only as a local forecast/fallback). Migration `0008_supplier_api_runs.sql` (owner must apply):
- `supplier_api_runs` — one durable row per run: provider, status (`running/completed/failed/exhausted`), requested_budget, hard_budget, reserved_points, list/detail/freight attempts, paid_retries, denied_attempts, timestamps. No secrets.
- `reserve_supplier_api_points(run_id, provider, action, cost, is_retry)` — atomic security-definer RPC. Reserves points ONLY when `reserved_points + cost <= hard_budget` (single UPDATE with row lock + re-evaluated WHERE — safe across multiple Vercel instances, cold starts, retries and concurrent requests); otherwise increments denied_attempts and returns `approved=false` — **the CJ HTTP request must not occur**. Also increments the matching attempt counter and paid_retries atomically. Refuses non-admin callers (`auth.jwt()` role check) — anon/customers can never touch it; RLS admin-only on the table; grants to authenticated/service_role.
- Server flow: `POST /api/suppliers/cj?action=start` creates the durable run (server clamps requested budget to the HARD MAX 250 — client/AI/query flags can never raise it) → search/detail/freight reserve against THAT persisted run id before every paid HTTP attempt (incl. every paid retry) → `budget` action reads authoritative usage → `finish` marks completed/failed/exhausted. Usage is labeled **AUTHORIZED/RESERVED point consumption** (every outbound paid request represented; CJ's exact billing not claimed). The engine starts/closes the run itself; if the server ledger is unavailable it degrades to client forecast with an honest warning (no false "global hard cap" claim).

**2. Market provenance is DB-verified — the persisted MARKET_INTELLIGENCE job is the source of truth.** `src/features/scout/marketProvenance.ts` `verifyMarketIntelligenceJob`: loads the job by the claimed `marketAnalysisId` and requires `type='MARKET_INTELLIGENCE'`, `status='completed'`, a finite 0–100 `output.marketScore`, a non-empty `output.evidenceFingerprint`, a parseable observed_at, and (when claimed) a fingerprint MATCHING the persisted job's. The engine derives marketScore / fingerprint / observed_at ONLY from the verified job — **a client-supplied marketScore is never trusted** (test: claimed 90, DB job 55 → effective 55). `runMarketIntelligenceJob` now persists `evidenceFingerprint` on the durable MI job output so later searches can prove grounding.

**3. Two CJ modes.** (A) **Market-grounded:** Product Scout stores the real MI job id + its recommended search hypotheses after an MI run; the owner picks one (auto-prefilled), and the CJ run carries `marketAnalysisId`/opportunity/hypothesis — the engine re-loads the job from the DB (test I: real MI job id, not a React-state number). May reach BUSINESS_QUALIFIED. (B) **Manual:** owner types any query with no MI link — Product Score / PRODUCT_SHORTLISTED work, Market Score UNKNOWN, BUSINESS_QUALIFIED always false.

**4. Qualification levels unchanged:** PRODUCT_SHORTLISTED = Product ≥75 + no hard rejection; BUSINESS_QUALIFIED = + Market ≥60 (DB-verified) + QA PASS + landed cost HIGH + USA delivery evidence. Market UNKNOWN ⇒ never business-qualified; Final Opportunity Score never bypasses the gates.

**5. Evidence-completion hook (§11) implemented minimally:** `evidenceCompletionGaps(record)` lists missing evidence categories (verified ratings, real variant-option semantics, origin/warehouse, weight, delivery window) for the strongest 3–6 only — deterministic, no extra web/AI calls; identity-matched external evidence required before it can ever raise a score.

**Simulation (fixtures, NO key) — all green:** A) two independent ledger instances share ONE durable budget (cross-instance); B) a fresh instance (cold start) sees previously reserved points; C) budget exhausted ⇒ DENIED and **zero paid CJ HTTP requests** (denial durably recorded); D) two concurrent 10-pt reservations with 10 remaining ⇒ exactly ONE succeeds; E) claimed 90 vs DB 55 ⇒ 55; F) fake MI job id ⇒ Market UNKNOWN, BUSINESS_QUALIFIED false; G) valid MI job + matching fingerprint ⇒ verified; H) manual query ⇒ shortlisted allowed, BUSINESS_QUALIFIED false; I) UI-style flow passes the real MI job id and the engine derives 65 from the DB; plus non-admin ledger caller rejected, 1000 clamped to 250, paid retries reserve their own points, fingerprint mismatch fails closed.

**Quality:** `npm test` ✅ **255/255** (16 new live-readiness tests) · `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅ (storefront 410.46 KB unchanged; admin lazy grows by the scout UI only). **Security:** CJ key NOT added, live CJ calls = 0, no browser secrets (`VITE_CJ_*` absent), ledger admin-JWT-only, credentials unchanged.

**Status:** **CJ ARCHITECTURE = PASS · MARKET BUSINESS-GATE LOGIC = PASS · DURABLE LEDGER CODE = PASS · MIGRATION 0008 = OWNER ACTION REQUIRED (safe version, not yet applied) · LIVE CJ PROOF = PENDING · CJ_API_KEY = NOT ADDED.**

## PHASE 4C — MIGRATION SECURITY + FAIL-CLOSED FIX (2026-08-17)

**Owner audit found 0008 (as first drafted) unsafe to apply. Fixed in place — the migration file is now the safe version the owner runs ONCE (never applied yet, so no 0009 needed):**

**1. FAIL CLOSED when the durable ledger is unavailable.** `runSupplierSearch` no longer warns-and-continues with a client forecast. If the durable supplier run cannot be created (unconfigured env, DB unreachable, permission denied, startRun absent/null), the run **STOPS** with `CJ_DURABLE_LEDGER_UNAVAILABLE`: zero listV2 / product/query / freightCalculate HTTP requests, a failed PRODUCT_RESEARCH job records the block truthfully, result carries `code: CJ_DURABLE_LEDGER_UNAVAILABLE`. The proxy refuses every paid action without a `runId` (no more local-forecast authorization path) and returns the same code when the ledger is unreachable. Client-side `CjPointBudget` is UX FORECAST ONLY and can never authorize a live paid request. (Regression-tested: ledger unavailable + CJ configured → `searchCalls/detailCalls/freightCalls` all 0; startRun returning null also fails closed.)

**2. DB-enforced hard cap.** `supplier_api_runs` now has CHECK constraints: `requested_budget`/`hard_budget` BETWEEN 50 AND 250, `reserved_points >= 0` AND `reserved_points <= hard_budget`, all attempt counters >= 0, `status IN ('running','completed','failed','exhausted')`, `provider IN ('cj')`. A direct INSERT/UPDATE can never store an out-of-range budget or negative counters.

**3. No direct counter/budget mutation.** Authenticated gets **SELECT only** (admin-only RLS SELECT policy); INSERT/UPDATE/DELETE are NOT granted to any role. All mutation goes through three SECURITY DEFINER RPCs: `start_supplier_api_run(p_provider, p_requested_budget)` — the DB (not the browser) stores the hard budget via `LEAST(GREATEST(requested,50),250)` (1000 → 250, tested); `reserve_supplier_api_points(p_run_id, p_provider, p_action, p_is_retry)` — the DB derives the cost (list=50/detail=10/freight=10), the caller can NEVER pass cost=0/-10/1, unknown actions raise an exception; `finish_supplier_api_run(p_run_id, p_status)` — allowed final statuses only, only a running row may finish. Parameter shadowing fixed: every arg is `p_`-prefixed, every WHERE compares `r.id = p_run_id` / `r.provider = p_provider` (no `provider = provider`). RPCs `SET search_path`, EXECUTE revoked from PUBLIC, granted to authenticated ONLY — **no service_role grants** (the functions' auth.jwt() admin check would reject service-role calls, so service_role support is not claimed). The server proxy now calls these RPCs (start/finish/reserve without cost); the old direct table POST/PATCH path is gone (mirrored in tests: direct INSERT/UPDATE → 403).

**4. Market query provenance (§10/§11).** `runMarketIntelligenceJob` now persists `recommendedSearchQueries` + `opportunity` on the MI job output. `verifyMarketIntelligenceJob` now ALSO verifies the ACTUAL `search.query` (normalized) against the persisted recommendations: match → `queryProvenance='verified'` and the DB score applies; no match (or no persisted recs) → effective Market Score = UNKNOWN, `BUSINESS_QUALIFIED=false`, PRODUCT_SHORTLISTED still allowed (manual CJ research works). Verified fields (opportunity, hypothesis, score, fingerprint, observed_at) come from the DB only — client labels are reported separately and never trusted. Persisted job metadata now includes `market_score_db`, `market_query_provenance`, `market_hypothesis` (matched DB recommendation, never the client's claimed hypothesis).

**Tests (274/274):** 19 new tests this closure — fail-closed A (zero paid HTTP) + A2 (null startRun); SQL contract (static, reads the migration file): [50,250] constraints, SELECT-only grant, no insert/update grants, SELECT-only RLS policy, reserve signature has NO cost param, DB literal costs, unknown-action rejection, LEAST/GREATEST clamp, p_ prefix/aliases, security-definer hygiene + no service_role; server RPC contract (mock mirrors the DB): DB-derived 10 for detail (wire carries p_action, no `cost`), 1000→250 clamp, 249+detail(10) → DENIED + zero CJ HTTP, invalid action → 400 + zero reservation, direct table POST/PATCH → 403, concurrent atomic; market query provenance K (matching query → VERIFIED + business-qualified), L (mismatched query → Market UNKNOWN, BUSINESS_QUALIFIED false, raw DB score still reported), M (client-claimed hypothesis not stored → ignored, DB opportunity used), normalizeMarketQuery. `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅.

**Security:** CJ key NOT added · live CJ calls = 0 · no browser secrets · no direct authenticated ledger mutation · credentials unchanged.

## PHASE 4C — LIVE CJ CONFIG + FIRST WINNER SEARCH (2026-08-17) — RUN EXECUTED, WINNER NONE (market gate)

**CJ key supplied by owner → configured, health ONLINE, controlled proof executed honestly.** Details below; the earlier "BLOCKED" entry (key absent) is superseded.

**Owner confirmed 0008 applied ✅. Live verification of 0008 (REST probes against the real project with the local `.env` keys — status/errors only, no secret values printed):**
- `supplier_api_runs` table exists (GET 200, empty).
- All three RPCs exist AND are admin-only: `start_supplier_api_run` / `reserve_supplier_api_points` / `finish_supplier_api_run` each return `400 P0001 permission denied for <fn>` when called with a non-admin JWT (service role) — the internal `auth.jwt()` admin check is live-enforced, exactly as designed.
- CHECK constraints live: INSERT `hard_budget=9999` → `23514` violation; INSERT `reserved_points=-5` → `23514`; INSERT `status='weird'` → `23514`; INSERT `hard_budget=100` → 201. Test rows cleaned up (residual rows = 0).
- **Residual-grant finding (real, fixed by 0009):** anon/authenticated still hold INSERT/UPDATE/DELETE *grants* on `supplier_api_runs` because Supabase project defaults grant table privileges DIRECTLY to those roles — 0008's `revoke all … from public` does not remove them. RLS (SELECT-only admin policy) already blocks the actual write (verified: anon PATCH on a REAL row → 204 with ZERO rows changed; row's `hard_budget`/`reserved_points` untouched), so security held — but the audit's "revoke direct mutation" requirement is now explicit via **`0009_revoke_supplier_runs_mutation.sql`** (`revoke insert, update, delete on supplier_api_runs from anon, authenticated;` — idempotent + transactional, **OWNER ACTION REQUIRED**, small; service_role keeps standard ops grants, never browser-used).

**CJ configuration — SUPPLIED and configured (2026-08-17).** `CJ_API_KEY` added to the Vercel **PREVIEW** env only (branch `luxedge-v2`, stored **Sensitive**, value never printed/committed/logged), Preview redeployed → `https://luxedge-website-q3h425zdl-8002salman-2281s-projects.vercel.app`. **Production unchanged** (no `CJ_API_KEY` in Production env — verified via `vercel env ls production`). No `VITE_CJ_*` anywhere. **0009 applied by owner ✅ and re-verified live:** anon PATCH on a real `supplier_api_runs` row now returns **401 42501 "GRANT UPDATE … TO anon"** (grant gone — previously silent 204), row unchanged, cleanup left 0 residual rows.

**Live controlled proof (real architecture — real preview serverless `/api/suppliers/cj` + `/api/ai/generate`, real Supabase DB, temp admin user created + deleted in `finally`):**
- **CJ Supplier API health = ONLINE** — real CJ authentication succeeded through the real proxy with the owner's key (cheapest safe probe, 0 retries).
- **Two grounded Market Intelligence passes** (DeepSeek via the secure router, real evidence): `pet enrichment` → **45/100** (deterministic 28 → DeepSeek 45), `dog travel accessories` → **55/100** (deterministic 28 → DeepSeek 55). Both persisted as MARKET_INTELLIGENCE jobs (`846747d7…` and `16a62942…`) with `recommendedSearchQueries` + `evidenceFingerprint` + `opportunity`.
- **MARKET OPPORTUNITY NOT STRONG ENOUGH** — both < 60 ⇒ **the CJ product search was NOT run** (spec §5 gate). **ZERO CJ product-search points consumed** (only the free auth/health call). No score fishing — exactly two attempts, never looped.
- **QUALIFIED WINNER = NONE.** Exact bottleneck: **market-opportunity evidence** — the grounded MI scores (45–55) fall below the required 60; CJ supplier data alone cannot create demand evidence, and lowering the threshold is forbidden. Product-side gates were never exercised because the market gate stops the run before any CJ spend.

**Live harness:** `src/features/suppliers/__tests__/live-cj-proof.test.ts` — guarded by `RUN_LIVE_CJ_PROOF=1` (skipped in normal `npm test`/CI; 274 passed + 1 skipped). Re-runnable by the owner: creates a temp admin via the service key, exercises the real preview proxy + real DB, deletes the temp admin in `finally`. Contains NO secrets (keys/URLs read from gitignored `.env` at runtime).

**Quality:** `npm test` ✅ **274 passed + 1 skipped (live harness)** · `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅. **Security:** key server-side only, Preview only, Production untouched, git/bundle/logs/DB contain no key, no `VITE_CJ_*`, temp admin user deleted (verified 200), credentials unchanged, main untouched (`4d0086d`).

**Status:** **MIGRATION 0008 = APPLIED ✅ + LIVE-VERIFIED · MIGRATION 0009 = APPLIED ✅ + LIVE-VERIFIED (write grants revoked) · LIVE CJ SUPPLIER INTEGRATION (auth/health/durable ledger) = PASS · LIVE CJ PRODUCT SEARCH = NOT RUN (market gate) · QUALIFIED BUSINESS WINNER = NONE (MI evidence 45–55 < 60) · CJ_API_KEY = CONFIGURED (Preview only).**

## PHASE 4D — MARKET INTELLIGENCE EVIDENCE UPGRADE (2026-08-18)

**Owner audit finding:** MI evidence was too thin because NO researched-page extracts were passed into Market Intelligence — `signalsFromDiscovery` supplied only DuckDuckGo URL counts + source/domain diversity. Fixed by building a real market evidence pack before DeepSeek scoring.

**New architecture (all honest — nothing invented):**
- `src/features/scout/marketEvidence.ts`: `selectEvidencePages` (max 6–8 STRONGEST exact product pages; product-marker URLs always win; rejects category/search/browse/directory/listicle patterns incl. `/s?k=`, `/b?node=`, `/b/`, `/product-category/`, `/-/n-`, `/lists/`, `-brands-in-the-usa`, `/top-N`, `/best-`, `made-in-the-usa` collection pages — the exact patterns the live DuckDuckGo discovery returns), `buildMarketEvidencePack` (fetch via the existing secure `/api/fetch-page` path + `extractPageFacts`; missing facts stay missing), `assessEvidenceQuality` (conservative CONFIGURABLE gate: ≥4 usable exact product pages, ≥3 independent domains, ≥3 price evidence, ≥3 availability, ratings "where obtainable" default 0).
- **Evidence-quality gate BEFORE DeepSeek:** a thin pack ⇒ `DEEPSEEK CALL = NO` + `MARKET EVIDENCE INSUFFICIENT` with the EXACT missing reasons (saves AI credits).
- `src/features/scout/marketDemand.ts`: provider-neutral `MarketDemandAdapter` interface + `NoopMarketDemandAdapter` (no credentials, no live calls); future Google Ads Keyword Planner hook prepared (avg monthly searches / competition / geo=US) — NO live calls, NO credentials required for Phase 4D.
- `engine.ts runMarketIntelligenceJob`: discovery → select → fetch → extract → signals (price_range / rating_evidence / availability now participate in the deterministic score) → quality gate → DeepSeek ONLY after the gate passes; persists per-job evidence metadata: `discoveredUrls, selectedUrls, successfulExtracts, failedExtracts, independentDomains, priceEvidenceCount, ratingEvidenceCount, availabilityEvidenceCount, evidenceQuality, evidenceQualityReasons`.
- ProductScout UI: the live/normal MI workflow now passes the real `fetchPage` (evidence pack is built, not skipped) and shows evidence quality in the MI modal.
- `extract.ts` honesty fix: the weak first-line title fallback now requires at least ONE real product signal on the page (price / availability / rating / reviews / shipping / origin / sizes / images) — a bare prose line is never fabricated into a title (this was silently counting non-product pages as usable).

**One controlled live proof (2026-08-18, query `dog travel accessories`, real preview `/api/fetch-page` + `/api/ai/generate`, real Supabase, temp admin created + deleted):**
- discovered 12 URLs → selected 3 (stronger category filter) → **3 extracts OK, 0 failed** (the fetch→parse→extract path is verified LIVE) → 2 independent domains, price evidence 0, ratings 0, availability 1.
- **Evidence quality = INSUFFICIENT** (need ≥4 usable pages, ≥3 domains, ≥3 price, ≥3 availability) ⇒ **DeepSeek NOT called** (the gate saved the AI credits as designed) ⇒ deterministic market score **33** (up from 28 — extracts now participate).
- Market gate NOT met ⇒ **CJ product search NOT run · ZERO CJ product points consumed · QUALIFIED WINNER = NONE.**
- Exact bottleneck now: **DISCOVERY SOURCE QUALITY** — DuckDuckGo generic discovery for this query returns listicles/browse pages (`allamerican.org/lists/*`, `gocattoys.com/made-in-the-usa`), NOT single-product retailer pages with prices. The selection filter now rejects those patterns, so a richer discovery source is required to feed the pack real exact product pages (e.g. the CJ official supplier API product records from Phase 4C, or site-restricted retailer discovery). This is honest: the pipeline is fixed and verified; the input source is the remaining gap.

**Tests:** 15 Phase 4D tests (selection incl. the live-discovery category patterns; pack-builder honesty; quality gate pass/fail with exact missing reasons; insufficient ⇒ zero `aiCall`; extracts raise the deterministic score; evidence metadata persisted; Noop/unconfigured/failing demand adapters never block; weak-title honesty) → **289 passed + 1 skipped** (live harness) · `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅.

**Status:** **MARKET EVIDENCE PACK = BUILT + LIVE-VERIFIED (fetch/parse/extract + gate) · MARKET-DEMAND ADAPTER INTERFACE = PREPARED (no credentials) · LIVE MARKET SCORE ≥60 = NO (33) · DEEPSEEK CALLS = SAVED (gate) · CJ PRODUCT POINTS CONSUMED = 0 · QUALIFIED WINNER = NONE.**

## PHASE 4E — RETAIL MARKET-EVIDENCE DISCOVERY (2026-08-18)

**Owner audit: fix domain diversity + availability semantics; build retailer-restricted evidence discovery.** All implemented and live-proved with ONE controlled run (query `dog travel accessories`, US).

**1. Domain round-robin selector (`selectEvidencePages`).** Now groups by domain and takes max ONE strong product URL per domain on the first pass, then a second page per domain as capacity allows (default maxPages=8, maxPerDomain=2) — one retailer can no longer occupy the whole pack when other independent domains exist. Regression tests: 5 Chewy + 2 Target + 2 Walmart with maxPages=6 ⇒ exactly 2 per domain and ≥3 domains; maxPerDomain enforced; exact rejection reasons (duplicate / category-search / browse / listicle / domain-quota) recorded per URL (`selectEvidencePagesDetailed` + `evidenceUrlRejectionReason`).

**2. Availability semantics fixed.** `availabilityEvidenceCount` now counts pages with EXPLICIT availability evidence (available OR unavailable); `availableCount` = positive availability only; `unavailableCount` = explicit out-of-stock; UNKNOWN availability counts in neither. The evidence-quality gate uses `availabilityEvidenceCount`; the market-opportunity availability scoring keeps using positive availability (`availableCount`). Tests: [available, unavailable, available] ⇒ 3 known / 2 available / 1 unavailable; UNKNOWN ⇒ neither.

**3. Retail evidence discovery (`src/features/scout/retailDiscovery.ts`).** Provider-neutral `RetailEvidenceDiscoveryAdapter.searchExactProducts({query, market, domains, maxPerDomain, maxTotal})` + `DuckDuckGoRetailDiscoveryAdapter`: site-restricted `site:<domain>` searches (one request per domain, max 8, no new proxies — uses the existing fetchRawPage infra). Initial domains chewy.com / target.com / walmart.com (+ optional petco/petsmart/tractorsupply). Verified single-product URL patterns (recorded, never sufficient alone): Chewy `/dp/<id>`, Target `/p/.../A-<id>`, Walmart `/ip/<id>`; STRICT pattern mode for those three (rich collection pages would otherwise pass content validation), permissive for future non-pattern domains. Returns candidates + rejected list with exact reasons + searchRequests/domainsAttempted/domainsWithResults.

**4. Exact-product page validation.** A fetched page counts as usable evidence only with a title AND ≥2 of {price, explicit availability, rating/review, real images, selectable variants, product attribute/brand} (`validateExactProduct`) — a listicle containing a price string is NOT usable. Rejection reasons persisted per URL (`rejectedUrls` on the pack).

**5. Market-demand honesty.** The search-breadth signal is now `search_breadth` (renamed from `search_pattern`) and is documented as **search-result breadth / market-supply visibility — NOT sales, orders, search volume, or consumer demand**; the limitation is passed explicitly in the DeepSeek prompt, and no score compensation is allowed. `rating_evidence`/`availability` signals carry review counts + known-vs-positive availability honestly.

**6. Live honesty fixes found by the proof.** (a) The fetch path counted Jina "Warning: … URL returned error 429" text as availability evidence — fixed in the shared client path (`isProxyErrorText` in importer.ts), the server proxy (`api/fetch-page.ts` now rejects it), and the harness: a rate-limited page is a FAILED fetch, never evidence. (b) A genuine server bug: `CjDurableRunLedger.start` read `rows[0].id` but the real RPC returns `run_id` (snake_case) — the durable-ledger start silently failed through the proxy (HTTP 200 + no runId) and was never caught because the Phase 4C CJ run never executed. Fixed (`run_id ?? id`), regression-tested by making the ledger mock mirror the real migration shape, and **verified LIVE**: the redeployed preview `/api/suppliers/cj?action=start` now creates a durable run (`runId` + `hardBudget:250`) with an admin JWT. Zero CJ points consumed.

**Live proof (ONE controlled run, retailDomains chewy/target/walmart, real preview + real DB, temp admin created + deleted):**
- retail discovery: 3 search requests · all 3 domains attempted · results on 1 (target.com) — the strengthened filters rejected **27** collection/search URLs with exact reasons (chewy `/b/` browse + `/deals/` + `/f/` filter; target `/s/` search + `/c/…/-/N-`; walmart `/c/kp/` + `/browse/` + `/c/lists/`).
- exact product URLs accepted: **1** — `target.com/p/petami-dog-travel-bag…/-/A-91561719` (extracted $39.99, available, 1 rating).
- evidence quality = **INSUFFICIENT** (need ≥4 usable pages / ≥3 domains / ≥3 prices) ⇒ **DeepSeek NOT called** (gate saved the credits) · deterministic market score **72** · **CJ search NOT run · CJ product points = 0 · QUALIFIED WINNER = NONE**.
- Exact source problem: **DuckDuckGo site-restricted indexing for these retailers returns browse/search/category pages, not exact PDPs, for this query** (Chewy/Walmart returned ZERO exact product URLs). This is the honest bottleneck — the evidence pack, selector, availability semantics, and gates all work; the discovery source cannot yet feed 4+ exact pages per hypothesis.

**Tests:** 305 passed + 1 skipped (live harness) — new Phase 4E suite: round-robin (A), maxPerDomain (B), availability known-vs-positive (C/D), listicle-with-price rejection (E), exact-product usable (F), discovery caps (G), category/search URL rejection incl. host-specific live patterns (H), gate pass at exactly 4/3/3/3 (I), gate-fail ⇒ zero aiCall (J), strict pattern mode + `run_id` regression. `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅. Preview redeployed (`luxedge-website-gbqr2hygw-8002salman-2281s-projects.vercel.app`) with the api/ fixes.

**Status:** **SELECTOR ROUND-ROBIN = PASS · AVAILABILITY SEMANTICS = PASS · RETAIL EVIDENCE DISCOVERY = BUILT + LIVE-VERIFIED · EXACT-PRODUCT VALIDATION = PASS · MARKET-DEMAND HONESTY = PASS · LIVE EVIDENCE GATE = INSUFFICIENT (honest) · DEEPSEEK CALLS = 0 (gate) · CJ PRODUCT POINTS = 0 · QUALIFIED WINNER = NONE.**

## PHASE 4E.1 — MARKET GATE FAIL-CLOSED + LEGACY STOREFRONT CATALOG RESET (2026-08-18)

**PART A — CRITICAL MARKET GATE CORRECTION (fail-closed). PASS.** An independent audit found the diagnostic deterministic score could leak into a qualification-capable Market Score. Fixed everywhere:

1. **Diagnostic vs qualifying score separated.** `runMarketIntelligenceJob` now sets: when `evidenceQuality = insufficient` → `marketScore = null` (NULL, not a number), `qualificationEligible = false`, `recommendedSearchQueries = []`, DeepSeek calls = 0 (unchanged), and the diagnostic score is persisted separately as `diagnosticDeterministicScore` + `diagnosticSuggestedQueries` (debugging only, labeled **DIAGNOSTIC SCORE — NOT QUALIFICATION ELIGIBLE** in the UI). Only when `evidenceQuality = sufficient` does `qualificationEligible = true`; DeepSeek may run and `marketScore` = the grounded AI score (deterministic fallback allowed ONLY because the gate passed). Threshold 60 unchanged.
2. **Persistence contract.** MARKET_INTELLIGENCE output now persists `diagnosticDeterministicScore` / `marketScore` / `qualificationEligible` / `evidenceQuality` / `evidenceQualityReasons` / `evidenceFingerprint` / `recommendedSearchQueries` / `diagnosticSuggestedQueries` / `aiUsed`. Insufficient runs persist `marketScore: null`, `recommendedSearchQueries: []` — diagnostic suggestions can never be accepted as supplier hypotheses.
3. **Market provenance FAILS CLOSED.** `verifyMarketIntelligenceJob` now also requires `output.evidenceQuality === 'sufficient'` AND `output.qualificationEligible === true` (plus the existing type/status/score/fingerprint/observed_at/query-match checks). A job is eligible market provenance ONLY when all hold — an insufficient-evidence job is rejected even if its diagnostic score is ≥60; a client-supplied marketScore can never rescue it.
4. **Product Scout UI no longer auto-arms CJ.** At the START of every new MI run the UI clears stale `miJobId`/`miHypotheses`/`cjHypothesis`/`cjMarketGrounded`. After MI completes, CJ is armed ONLY through the new shared `cjMarketContextFor(result)` helper when `qualificationEligible && marketScore >= 60 && recommendedSearchQueries.length > 0`; otherwise the UI shows **MARKET NOT ELIGIBLE FOR SUPPLIER QUALIFICATION** (diagnostic score still visible for audit). An earlier passing MI job can never remain silently armed after a later failed/insufficient run.
5. **Tests A–H added (7 new):** 1-page diagnostic 72 → marketScore null + qualificationEligible false; persisted insufficient job rejected by provenance + client 95 ignored; insufficient → qualification queries []; sufficient + deterministic fallback → eligible with gated deterministic score; sufficient + DeepSeek 68 → marketScore 68 + CJ arm; new insufficient run never inherits prior arming; high-diagnostic insufficient run never arms CJ. Existing Phase 4D/4C fixtures updated to the sufficient-evidence contract (312 passed + 1 skipped).

**PART B — CURRENT STOREFRONT PRODUCT CLEANUP (owner-authorized). DONE.**

**Live DB audit (REST probes, real project, no secrets printed):** products table = **1 row** — `KONG Classic, Durable Natural Rubber Dog Toy` (`edd86a77-…`, status `published`, $11.96 USD, slug `kong-classic-dog-toy`) — the Phase 3B E2E seed product; product_images = **4** (all for that product); orders = **0**, order_items = **0**; no carts/wishlists tables exist; creative_assets / reviews / fulfillment / payments / collection_products = 0 rows; no table references the product id. Hardcoded/fallback frontend products: **YES — removed** — the storefront previously fell back to `ALL_PRODUCTS` (≈144 demo cards: 24 INIT + ~120 EXTRA, including non-pet items like toothbrushes/dumbbells/LED strips) whenever the DB had zero published products; the `My Orders` demo rows are keyed to fake user ids and are invisible to real customers (verified).

**Mutation executed (authorized by owner):** (1) KONG Classic → **status = `draft`** (removed from the active storefront; row kept for history — the `product_candidates` row `b7280215-…` (approved) + supplier/score/evidence trail preserved). (2) **4 product_images deleted** (safely unreferenced — zero order/FK dependencies; product row retained so no FK breaks). (3) Verified: published products = **0** via service key AND the anon/RLS storefront path; product_images = 0. NO hard-delete of the product row (audit/history retained per spec §7).

**Empty-catalog experience (luxedge-v2 only):** the storefront now starts with an EMPTY catalog (no demo fallback) and shows a premium empty state — home: "New premium pet essentials are being curated" (replaces empty Best Sellers / Pet Parent Favorites grids); shop: same curation notice (distinct from the filter-results "No products found" state); PDP already degrades to "Product Not Found". **Verified in the browser** (local preview, real Supabase): home + shop render the premium empty state, zero fake cards, category counts 0, no JS errors. This change is on `luxedge-v2` ONLY — **NOT deployed to production** (`main` still shows the demo fallback when the DB is empty). **Owner approval required to ship the empty-catalog change to main/production** (per spec §9: main must remain untouched without approval); the unpublish itself is technically safe on main (it degrades to the demo catalog, no broken UI) — but the demo fallback removal requires the frontend deploy.

**PART C — HIGGSFIELD PLACEHOLDER POLICY.** HIGGSFIELD = **PLAN PENDING** (owner has not purchased/selected a plan): no key added, zero API calls, no fake creatives. Recorded future path: Qualified product → owner review → Higgsfield creative phase → creative QA → storefront publish. Only a future BUSINESS_QUALIFIED + OWNER-APPROVED product should consume Higgsfield credits.

**PART D — CJ.** No listV2 / product/query / freightCalculate this task. **CJ product points consumed = 0.** (Durable ledger untouched; no paid supplier calls.)

**PERMANENT STOREFRONT RULE (documented + enforced).** A product must NOT become customer-visible merely because it exists in `products` or is PRODUCT_SHORTLISTED. The storefront loader (`catalog.ts`) renders ONLY `status='published'` rows, and the future publish path requires BUSINESS_QUALIFIED + QA PASS + OWNER APPROVED + listing factual QA PASS + creative/image quality gate PASS + explicit publish authorization. **AUTO PUBLISH = OFF** (engine only ever creates drafts — regression-tested).

**Quality:** `npm test` ✅ **312 passed + 1 skipped (live harness)** (7 new Phase 4E.1 gate tests; CJ provenance fixtures updated to the sufficient-evidence contract) · `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅ (storefront 412.60 KB / gzip 117.22 KB — empty-state markup is small).

**Security:** no credentials exposed or changed; CJ key still Preview-only server-side; no `VITE_CJ_*`; git/bundle/logs/DB clean; Production env unchanged; probe scripts removed.

**Status:** **MARKET GATE FAIL-CLOSED = PASS · INSUFFICIENT EVIDENCE MARKET SCORE = NULL · LEGACY ACTIVE CATALOG = CLEARED (1 published → 0; 4 images removed; draft row + candidate/evidence preserved) · EMPTY-CATALOG EXPERIENCE = BUILT + VERIFIED (luxedge-v2 only) · HIGGSFIELD = PLAN PENDING / KEY NOT ADDED / CALLS 0 · CJ PRODUCT POINTS THIS TASK = 0 · QUALIFIED BUSINESS WINNER = NONE.**

## PHASE 4E.2 — PRODUCTION CATALOG HOTFIX PREVIEW + RETAIL PDP NAVIGATION DISCOVERY (2026-08-18)

**PART A — PRODUCTION STOREFRONT CATALOG HOTFIX (preview only, main untouched). DONE.**

- Created `luxedge-catalog-reset-hotfix` from **main @ `4d0086d`** (exact base) and ported ONLY the minimal customer-storefront reset — no Phase 4C/4D/4E architecture, no CJ code, no migrations, no admin refactors. Files changed on the hotfix: `src/App.tsx` only (**TEMPORARY EMPTY-CATALOG LOCK — the hotfix does NOT load a Supabase catalog**; zero demo products reachable; premium empty state; fake social-proof removed). *Documentation correction (merge phase below): the earlier "storefront source of truth stays Supabase" claim was WRONG for this branch — main has no Supabase integration; the hotfix intentionally renders ZERO products; future published DB products will NOT auto-appear through it (the full DB-driven storefront ships via the later approved luxedge-v2 production release).*
- Removed production demo fallback + fake trust copy: main's hero previously claimed **"4.9/5 (2,000+ happy pet parents)"** and About claimed **"12,000+ pets… 5,000+ 5-star reviews"** — these were unverified demo claims (banned by the spec + Master Plan §14). Replaced with safe copy. Demo `INIT_PRODUCTS`/`ALL_PRODUCTS` constants remain as dev fixtures but are **unreachable on the customer storefront path**. *Documentation correction (see Phase 4E.2B + Part G of Phase 4F): this hotfix NEVER loads a Supabase catalog — the products state is permanently `[]` (TEMPORARY EMPTY-CATALOG LOCK). The `{ products: [], categories, source: 'supabase' }` empty-real-catalog contract belongs to luxedge-v2's `catalog.ts` (Part B item 3 below), NOT to the hotfix branch.*
- Empty-catalog UX ported in smallest safe form: home "New premium pet essentials are being curated", shop clean empty state, old PDP → Product Not Found, category clean. No fake launch date / counts / reviews / sales. Trust copy in the empty state uses safe wording only (no invented shipping/return policies).
- **Verified (deployed preview + local dev serving the same code):** `/` home + `/#/shop` + category + old KONG PDP route all 200; visible demo products = 0; non-pet products = 0; legacy bad images = 0; console errors = 0; the only demo-looking names remaining were stale DOM inside the off-screen cart drawer (hidden, not rendered). Desktop + mobile checked.
- Deployments: first preview `luxedge-website-8izk9y5yo-…`, final (after fake-claim removal) `luxedge-website-i2zlgvdoo-8002salman-2281s-projects.vercel.app`. **MAIN NOT MERGED — `main` still `4d0086d`.** Ready for owner production approval (no owner action needed on data — KONG already draft).

**PART B — RETAIL PDP NAVIGATION DISCOVERY (luxedge-v2). BUILT + LIVE-VERIFIED.**

1. **Retailer listing pages are now NAVIGATION sources only** — a retailer search/category/collection page contributes ZERO price/rating/availability evidence or Market Score points; it is used only to extract exact PDP links (`RetailNavigationAdapter` → `retailNavigation.ts`): `discoverProductLinks({sourceUrl, retailer, query, maxLinks})` returns PDP URLs + rejection reasons, no credentials. The listing page itself never enters the evidence pack.
2. **Flow:** retailer listing/search page → fetch → extract exact PDP links → retailer-specific URL validation (Chewy `/dp/`, Target `/p/…/A-`, Walmart `/ip/`; strict mode for the three verified retailers) → fetch PDP → content-level exact-product validation (title + ≥2 of price/availability/rating/images/variants/origin) → only real PDPs enter `MarketEvidencePack`. Discovery budget respected: 1 hypothesis only (`dog travel accessories`), ≤3 listing fetches, ≤8 PDP candidates, ≤2 per retailer.
3. **Empty-real-catalog contract (`catalog.ts`):** DB reachable + 0 published → `{ products: [], categories: […], source: 'supabase' }`; DB error → error (no demo fallback). Stale comments removed; tests updated + added (empty DB → empty products, demo never renders, DB error → no fake fallback, old PDP → safe unavailable).
4. **Honesty fix found by the live proof:** Jina Reader returns BLOCKED-SNAPSHOT pages ("## www.target.com is blocked … ERR_BLOCKED_BY_CLIENT") when its own crawl was blocked — previously mislabeled as a "fetched page with 0 signals". Now detected in `isProxyErrorText` (client) and `api/fetch-page.ts` (server proxy) as a FAILED fetch. Regression tests added.
5. **Live proof (ONE controlled run, real preview + real DB, temp admin created + deleted, `RUN_LIVE_CJ_PROOF=1`):**
   - retail discovery: 3 search requests · chewy/target/walmart attempted · results on 1 domain (target.com) · 27 collection/search URLs rejected with exact reasons (unchanged from 4E).
   - **retail navigation: 3 listing/search pages fetched as NAVIGATION sources → 8 exact PDP URLs extracted** (Target listing → real `/p/…/A-` PDPs, e.g. PetAmi travel bag + console dog car seat). Navigation works.
   - PDP fetches: **1 failed (Jina block snapshot — now honestly counted as failed)** · 1 fetched but 0 of 2 required product signals (thin snapshot) → rejected with reason.
   - evidence quality = **INSUFFICIENT** (0 usable pages / 0 domains / 0 prices / 0 availability) ⇒ **DeepSeek NOT called · Market Score = NULL (fail-closed) · CJ search NOT run · CJ product points = 0 · QUALIFIED WINNER = NONE**.
   - **Exact remaining bottleneck:** Target PDPs are not retrievable through the current proxy right now (Jina returns blocked/thin snapshots; `SCRAPE_DO_TOKEN` is NOT configured on the preview). Navigation + selection + validation + gates all work; the fetch layer for retailer PDP content is the blocker.

**Tests:** `npm test` ✅ **327 passed + 1 skipped (live harness)** — new: navigation link extraction per retailer (Chewy/Target/Walmart), dedupe, max 2/domain, listing-page-not-evidence, invalid URL rejection after extraction, gate-fail ⇒ 0 DeepSeek; empty-catalog contract tests; block-snapshot detection tests. `npx tsc --noEmit` ✅ · `npm run build` ✅. Preview redeployed with the api/ fixes.

**Status:** **PRODUCTION HOTFIX = BUILT + PREVIEW-VERIFIED (main untouched, owner approval needed to ship) · RETAIL NAVIGATION DISCOVERY = PASS (8 PDP links from listing pages) · LISTING-PAGE-NOT-EVIDENCE = PASS · BLOCK-SNAPSHOT HONESTY = PASS · LIVE EVIDENCE GATE = INSUFFICIENT (honest) · DEEPSEEK = 0 · CJ PRODUCT POINTS = 0 · QUALIFIED WINNER = NONE · HIGGSFIELD = PLAN PENDING / CALLS 0.**

## PHASE 4E.2B — PRODUCTION CATALOG RESET HOTFIX: MERGED TO PRODUCTION + LIVE QA + STATE SYNC (2026-08-18)

**OWNER APPROVAL + MERGE (fast-forward only).** Owner approved merging `luxedge-catalog-reset-hotfix` into production `main`. Pre-merge safety verified: `origin/main = 4d0086d`, hotfix tip `b62090f` on main's lineage (3 commits, changed files `src/App.tsx` + `.gitignore` only — no V2 architecture/CJ/migrations). Merged **fast-forward `4d0086d → b62090f`** (no squash, no unrelated commits), pushed. **NEW MAIN SHA = `b62090f`** (`origin/main` = `b62090f`). `luxedge-v2` untouched (`7c6ff66`).

**PRODUCTION DEPLOY.** The main push triggered the normal Vercel Production build; reached **READY** (`dpl_BQayx3vSyyfNiQTU6JXMn8cfBWnG`, url `luxedge-website-na9pix3rv-8002salman-2281s-projects.vercel.app`, aliases `luxedge.us` + `www.luxedge.us`). No Production env vars modified.

**HOTFIX ARCHITECTURE TRUTH (documentation correction).** The Phase 4E.2 text above incorrectly implied the hotfix was Supabase-catalog-driven. Truth, now corrected:
- HOTFIX CATALOG MODE = **TEMPORARY EMPTY-CATALOG LOCK** (products state permanently `[]`; the customer storefront renders ZERO products).
- HOTFIX SUPABASE PRODUCT LOADING = **NO** (main has no Supabase client/catalog integration; this emergency branch intentionally does not load one).
- FUTURE DB-PUBLISHED PRODUCT AUTO-DISPLAY THROUGH HOTFIX = **NO** — requires the later separately-approved luxedge-v2 production release (which has the real `catalog.ts` Supabase loader + empty-catalog contract).
- Demo `INIT_PRODUCTS`/`ALL_PRODUCTS` remain only as admin/dev fixtures — unreachable on the customer path (the PDP renders from the empty products state → Product Not Found; `ALL_PRODUCTS.find` is used only for the SEO document title).

**LIVE LUXEDGE.US QA (production, verified).** All routes 200 (`/`, `/#/shop`, `/#/category/pet-toys`, old `/#/product/kong-classic-dog-toy`, `/#/cart`, `/#/checkout`→redirects to cart, `/#/about`, `/#/contact`, `/ads.txt`). Deployed bundle (`assets/index-CvGrIlzN.js`, 406,501 B) verified: empty-state copy present, **zero banned claims** (no "4.9/5", "2,000+ happy pet parents", "12,000+ pets", "Free shipping $50+", "30-day returns", support hours, hassle-free/Stripe/PayPal-processed copy). Local `npm run build` produced the **identical bundle hash** — byte-for-byte confirmation that production serves the QA'd code. Browser QA (same commit served locally): visible product cards = 0, demo products = 0, non-pet products = 0, old KONG PDP = Product Not Found, home + shop premium empty state, cart badge 0 / drawer empty, console errors = 0.

**LEGACY CART MIGRATION (live).** Planted an old demo payload under `luxedge_cart` + a stale payload under `luxedge_cart_v2` (SonicGlow id 13 + PawPals id 7), reloaded: legacy key **removed**, v2 reconciled to **[]**, badge "0 items", demo names/images invisible, checkout blocked (route redirect + `placeOrder` returns '' → no order, no `purchase` event, no success screen). Test localStorage cleaned up afterwards.

**POLICY/TRUST + ADSENSE.** All Phase 4E.2A-removed claims absent from the live bundle. Pre-existing (NOT hotfix-introduced, flagged for owner awareness): the footer "We accept: VISA MC AMEX PayPal Apple Pay" bar + the demo checkout card/PayPal UI remain on main — checkout is a simulated demo flow; the FAQ honestly states "This preview storefront does not process real payments yet" and this state doc records checkout is demo mode. Ads/AdSense preserved byte-identical (the hotfix touched only `src/App.tsx` + `.gitignore`): `index.html` AdSense ownership meta (`ca-pub-5473713135927706`) + `adsbygoogle` script intact live, `/ads.txt` serving the ownership line, cookie/consent behavior untouched. No Google Ads campaigns launched (blocked pending a real checkout/payment system).

**QUALITY.** Merged production code: `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅ (main has no test runner — reported honestly; behavior QA done live above).

**STATE.** Production hotfix deployed (new main SHA `b62090f`) · live product count = 0 · legacy cart purge verified · fake demo catalog removed from production · Higgsfield = PLAN PENDING / key absent / calls 0 · Google Ads launch blocked pending real checkout · **CJ product points still 0** (no CJ calls this task; the temporary CJ Preview credential untouched) · luxedge-v2 feature code unchanged (`7c6ff66`, docs-only commit for this sync).

## PHASE 4F — CJ SEED DISCOVERY → TARGETED MARKET EVIDENCE → CONDITIONAL QUALIFIED CJ SEARCH (2026-08-18)

**Mission achieved honestly:** the web-discovery bottleneck was proven and attacked at the source — instead of repeating retailer-restricted web searches, the OFFICIAL CJ supplier API was used ONCE as a LOW-COST PRODUCT-CONCEPT SEED SOURCE (50 points), and the CJ-learned concept vocabulary produced the FIRST SUFFICIENT market-evidence pack of the project. The market gate then correctly stopped the conditional CJ qualification run (grounded Market Score 45 < 60). No winner fabricated; zero research points spent.

**PART A — CJ_SEED_DISCOVERY (built + LIVE-proven).** New `src/features/scout/cjSeedDiscovery.ts`:
- `runCjSeedDiscovery` — a clearly separate workflow (NOT BUSINESS_QUALIFIED / PRODUCT_SHORTLISTED / MARKET-GROUNDED). Creates ONE durable `supplier_api_runs` run with a hard budget of **exactly 50** (ONE product/listV2 call); NO product detail, NO freightCalculate; a failed list call STOPS the run (no second paid seed run). FAIL CLOSED on ledger unavailability (CJ_DURABLE_LEDGER_UNAVAILABLE, zero paid calls). Persists the seed proof durably on the existing **PRODUCT_RESEARCH** job type (output.mode = 'CJ_SEED_DISCOVERY', no schema migration needed) and NEVER creates product_candidates, scores, drafts or publishes.
- **Seed record truth:** only factual CJ fields (pid, sku, title, category, image, base price, US inventory figure/verified/in-country, warehouse, delivery cycle, `listedNum`, observed_at). No ratings/reviews/sales/orders/trends/freight/landed cost invented. `listedNum` is preserved as a **supplier listing count** and the job output explicitly records `demandEvidence: 'NONE — CJ supplier data is not market demand.'`; `marketScore = null`, `qualificationEligible = false` on the seed job.
- **LIVE (one controlled run, real CJ, real durable ledger, temp admin created + deleted):** run `cd8f63b8-…` — hard budget **50**, reserved **50**, **list_attempts 1, detail_attempts 0, freight_attempts 0, paid_retries 0, denied 0**, status `exhausted` (as designed); **40 real supplier records** returned, 40 seeds, 0 duplicates. Seed job `b01c2362-…` completed. (This was the project's FIRST real CJ product/points spend — the ledger enforced every rule.)

**PART B — PRODUCT-CONCEPT CLUSTERING (built + live).** Deterministic (no AI): `conceptKey`/`conceptTokens` (punctuation/size/qty/dimension/stop-word stripping), greedy Jaccard-token clustering (`clusterCjSeedConcepts`, threshold 0.5) so obvious variants of one concept group together, and `selectSeedConcepts` ranks at most **2** concepts by SUPPLIER-SUITABILITY only (pet relevance, title clarity, real price, images, US-inventory figure, delivery cycle, and the existing IP/medical/battery safety prefilter) — never invented popularity. `seedConceptSearchQuery` derives the targeted market-search vocabulary from the ACTUAL CJ title vocabulary (≤7 tokens, no invented numbers). Live clusters: (1) elevated dog sofa bed (3 pids, suitability 15), (2) PawHut dog ramp for bed (2 pids, suitability 12).

**PART C+D — TARGETED MARKET EVIDENCE + GATE (LIVE — FIRST SUFFICIENT PACK).** The harness tried concept #1 (pet bed) then, ONCE, concept #2 (dog ramp) using the CJ-learned vocabulary (NOT restricted to Target/Chewy/Walmart — Amazon/PawHut/Target/Lowes/Home Depot/Walmart all surfaced). Discovery limits respected (≤6 search requests, ≤8 PDP candidates, ≤2/domain). Concept #2 produced the project's first **SUFFICIENT** evidence pack: **5 usable exact PDPs · 4 independent domains · 3 prices · 5 explicit availability (4 available) · 1 rating + 40 observed reviews** (PawHut dog ramp — same SKU family across retailers). Fetch honesty kept (429/403/blocked/thin snapshots = failed fetches). Evidence gate passed ⇒ exactly ONE grounded DeepSeek Market Intelligence call ran; **diagnostic deterministic score 64, QUALIFYING grounded Market Score 45** (DeepSeek scored lower than the diagnostic — no inflation).

**PART E — CONDITIONAL QUALIFIED CJ RUN (fail-closed, NOT started).** `conditionalQualifiedRunDecision` requires evidenceQuality sufficient + qualificationEligible + Market Score ≥ 60 + a persisted recommendation; here **Market 45 < 60 ⇒ run:false ('market-score-below-gate') — the 250-pt qualification run was NOT started, ZERO research points consumed.** Had it started, the actual query would have had to match a persisted MI recommendation (marketProvenance verifies from the DB).

**PART F — PRODUCTION TODO (recorded, production untouched).** Before Google Merchant Center / Ads launch: the current production footer still contains "**We accept: VISA MC AMEX PayPal Apple Pay**" — the real payment processor is not configured. Those acceptance badges must be removed or replaced with the actually-verified payment methods when the real checkout is integrated. **No production change made in this phase** (main = `b62090f`).

**PART G — STATE DOC CLEANUP (done).** The stale Phase 4E.2 sentence that described the Supabase empty-real-catalog contract as hotfix behavior is corrected above: the hotfix is a TEMPORARY EMPTY-CATALOG LOCK with NO Supabase product loading; the `{ products: [], categories, source: 'supabase' }` contract belongs to luxedge-v2's `catalog.ts`.

**Tests:** `npm test` ✅ **343 passed + 1 skipped (live harness)** — 16 new Phase 4F tests (A: seed budget exactly 50; B: max one listV2; C: zero detail/freight; D: listedNum not demand; E: no candidate creation; F: clustering dedupes variants; G: max 2 concepts; H: insufficient ⇒ DeepSeek 0 / marketScore null / no run; I: Market <60 ⇒ no run; J: Market ≥60 + persisted rec ⇒ run allowed; K: query mismatch ⇒ Market UNKNOWN / not business-qualified; L: paid detail calls reserve via ledger; + fail-closed ledger, failed-list stop, concept-vocabulary). `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅.

**Security:** CJ_API_KEY server-side Preview only, never printed/logged/committed; no `VITE_CJ_*`; no SCRAPE_DO_TOKEN; credentials unchanged; Production untouched (main `b62090f`); probe scripts removed; temp admin deleted (HTTP 200).

**Status:** **CJ SEED DISCOVERY = BUILT + LIVE-PROVEN (50-pt ledger, ONE list, zero detail/freight) · CONCEPT CLUSTERING = PASS · TARGETED MARKET EVIDENCE = FIRST SUFFICIENT PACK (5 pages / 4 domains / 3 prices) · DEEPSEEK = 1 grounded call (Market 45, diagnostic 64) · MARKET GATE = FAIL-CLOSED (45 < 60) · CONDITIONAL CJ QUALIFICATION RUN = NOT STARTED · RESEARCH POINTS = 0 · QUALIFIED WINNER = NONE · HIGGSFIELD = PLAN PENDING / CALLS 0 · main = `b62090f` (untouched).**

## PHASE 4G — DIRECT SEARCH-DEMAND EVIDENCE + CJ SEED ACCOUNTING HARDENING (2026-08-18)

**Mission:** harden CJ seed accounting (server-authoritative, not client forecast), remove the concept cluster-size bias, enforce exact-product identity rules for review evidence, and BUILD (not activate) the Google Ads Keyword Planner demand-adapter architecture. **NO live calls in this phase** — zero CJ points, zero DeepSeek, zero Google Ads.

**PART A — SERVER-AUTHORITATIVE CJ POINT ACCOUNTING (done).** `runCjSeedDiscovery` no longer trusts the local `CjPointBudget` for reported usage. After EVERY paid seed attempt — SUCCESS OR FAILURE — it fetches `adapter.getRunUsage()` (the durable `supplier_api_runs` ledger) and persists/reports separately: `serverAuthorizedPoints`, `serverRemainingPoints`, `serverListAttempts`, `serverDetailAttempts`, `serverFreightAttempts`, `serverPaidRetries`, `serverDeniedAttempts` + `clientForecastPoints` (diagnostics only) + `runId`/`hardBudget`. The deprecated `pointsReserved` alias is explicitly the client forecast. Correct wording: **SERVER-AUTHORIZED / RESERVED points** (never "exact CJ billing"). Regression test A: server ledger reserves 50, response lost before local budget spend → reports `serverAuthorized=50, clientForecast=0, transportStatus='failed/unknown'` — never 0.

**PART B — CONCEPT SIZE-BIAS REMOVED (done).** `assessConceptSuitability` now awards a FIXED concept-level score (pet relevance 0–2, clear concept 0–2, verified price 0–2, usable image 0–2, US-inventory figure 0–2, delivery-cycle 0–1, basic safety 0–2, researchability 0–2; MAX 15). Points are awarded once per concept, NEVER per member record — 5 near-duplicate variants score the same as 1 complete record. `memberCount` is informational only (never popularity); `listedNum` contributes nothing. `selectSeedConcepts` tie-breaks by label (deterministic), not member count. Tests C + D prove equal scores for 5-vs-1 concepts and zero `listedNum` influence.

**PART C — PRODUCT IDENTITY HONESTY (done).** New `src/features/scout/identity.ts`: `PageIdentityEvidence` (brand, model, MPN, SKU, UPC/GTIN, canonicalTitle — unknown = null), extracted conservatively from page text / JSON-LD (`extract.ts`). `identityMatchLevel` = `exact` (shared UPC/GTIN, shared MPN, or brand + exact model/SKU) / `concept` (title-token overlap ≥0.5 — SAME CONCEPT, NOT exact) / `unknown`. `aggregateReviewEvidence` NEVER sums review counts across non-exact identities — it reports "X market PDPs carried review evidence" + exact-identity-group count + concept/unknown pages, and persists `reviewAggregationNote` + `identityEvidencePages` on the job/pack. Tests E–G.

**PART D — GOOGLE ADS KEYWORD PLANNER ADAPTER (BUILT, NOT LIVE).** `src/features/scout/marketDemand.ts` rewritten: provider-neutral `MarketDemandAdapter` + structured `DemandCollectionResult` (`status: not_configured | success | error`, `errorSafe` scrubbed, observedAt, keywords, avgMonthlySearches, monthlyHistory, competition/index, bidRangeUsd). `GoogleAdsKeywordPlannerDemandAdapter` parses official `KeywordPlanIdeaService`/`GenerateKeywordHistoricalMetrics` fragments (snake/camel), converts to `MarketSignal`s (`search_demand`, `search_demand_history`, `keyword_ad_competition`, `keyword_bid_range`) each with explicit limitations ("search-volume evidence … does not prove purchases, conversion rate, marketplace sales, or Luxedge profitability"; competition = AD-SLOT competition). Hard cap `GOOGLE_MAX_KEYWORDS = 5`; geo USA / language en; same-month cache by normalized keywords+geo+language. **A configured-but-failed provider now reports explicit `status:'error'` — never silently `[]`** (the Phase 4D no-op tests were updated to the structured contract). `CJ listedNum` can never enter this adapter (input shape has no such field). Tests H–O.

**PART E — SERVER ROUTE (built, secret-safe, no live calls).** `/api/market-demand/google-ads` (admin JWT, rate-limited): `health` → `configured | not_configured` (presence only, no secrets) and `historical-metrics` → validates ≤5 keywords / USA / English, then Phase 4G returns `not_configured` (fail safe — NO live Google call even if credentials were present). `api/_lib/googleAds.ts` defines the env contract (`GOOGLE_ADS_DEVELOPER_TOKEN`, `GOOGLE_ADS_CLIENT_ID`, `GOOGLE_ADS_CLIENT_SECRET`, `GOOGLE_ADS_REFRESH_TOKEN`, `GOOGLE_ADS_CUSTOMER_ID`, optional `GOOGLE_ADS_LOGIN_CUSTOMER_ID`) — **no values added anywhere; no VITE_*; never in DB/state/logs**.

**PART F — MI INTEGRATION + UI (done).** `runMarketIntelligenceJob` persists `demandProvider / demandStatus / demandErrorSafe / keywordsRequested / keywordsReturned / avgMonthlySearches / demandMonthlyHistory / keywordAdCompetition / competitionIndex / bidRangeUsd / demandObservedAt` on the durable job and returns a `demand` block on the result. Product Scout's Market Intelligence modal shows a **DIRECT DEMAND DATA** panel: `NOT CONFIGURED / ERROR / EVIDENCE COLLECTED` with factual metrics only — never WINNER/TRENDING/HIGH DEMAND labels from one metric.

**§8 — MISLEADING NAMING FIXED (done).** The deterministic market breakdown criterion is renamed to **`supplyVisibility`** ("market-supply visibility … not sales/search volume/consumer demand"); `breakdown.demand` remains only as a `@deprecated` compatibility alias pointing at the same object (total score sums `supplyVisibility` — no double count, weights unchanged). Test updated.

**Tests:** `npm test` ✅ **361 passed + 1 skipped (live harness)** — 18 new Phase 4G tests A–O (server-authoritative accounting incl. failure path; concept size-bias; listedNum isolation; exact/concept identity; review-aggregation honesty; not-configured zero-call; metrics parsing; env-name-only secret check; 5-keyword cap; configured-failure safe error; same-month caching; MarketSignal limitations; listedNum↛adapter). `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅ · bundle scan: only env-var NAMES (config references), never values; `.env` gitignored; no secrets in diff.

**LIVE CALLS THIS PHASE = 0** (CJ points 0 · DeepSeek 0 · Google Ads 0 · Higgsfield 0). **Security:** no new real credentials; existing CJ_API_KEY unchanged (Preview only, server-side); Production env unchanged; main untouched.

**Status:** **PHASE 4G CODE = PASS · CJ SERVER-AUTHORIZED ACCOUNTING = PASS · CONCEPT SIZE BIAS = REMOVED · EXACT IDENTITY RULES = PASS · GOOGLE ADS DEMAND ADAPTER = BUILT / NOT CONFIGURED · LIVE GOOGLE DEMAND PROOF = NOT RUN · CJ POINTS THIS PHASE = 0 · DEEPSEEK CALLS THIS PHASE = 0 · HIGGSFIELD CALLS = 0 · main = `b62090f` (untouched).**

### OWNER SETUP NEEDED FOR LIVE GOOGLE DEMAND PROOF (non-secret steps only)
1. Create/use a Google Ads account + enable the **Google Ads API** (developer token — typically requires an approved dev-token application).
2. Set the six server-side env vars on **Vercel Preview only** (Project → Settings → Environment Variables): `GOOGLE_ADS_DEVELOPER_TOKEN`, `GOOGLE_ADS_CLIENT_ID`, `GOOGLE_ADS_CLIENT_SECRET`, `GOOGLE_ADS_REFRESH_TOKEN`, `GOOGLE_ADS_CUSTOMER_ID`, optional `GOOGLE_ADS_LOGIN_CUSTOMER_ID`. Never `VITE_*`, never browser/localStorage/DB/state docs.
3. Redeploy the preview; hit `/api/market-demand/google-ads?action=health` with the admin JWT → expect `health: 'configured'`.
4. Then authorize ONE controlled proof request (≤5 keywords, geo USA, language en) → `historical-metrics` calls the official Keyword Planner endpoints (rate-limited; one request per MI proof; same-month caching).
5. The grounded MI pipeline then includes official search-volume signals in the evidence fingerprint + DeepSeek pack; Market Score threshold stays 60; CJ qualification run stays conditional. No Ads campaign, no Merchant Center, no production change until separately approved.

## PHASE 4G.1 — GOOGLE ADS LIVE-PATH CORRECTNESS CLOSURE (2026-08-18)

**Mission:** make the Google Ads demand path REAL-CAPABLE so the owner can add credentials later and run ONE controlled proof without an architecture rewrite — using MOCKED official v25 responses only. **NO live calls this phase** (Google 0 · CJ 0 · DeepSeek 0 · Higgsfield 0).

**PART A — SERVER ROUTE REAL-CAPABLE (done).** `api/_lib/googleAds.ts` rewritten: `GOOGLE_ADS_API_VERSION = 'v25'` (centralized); **OAuth refresh** (GOOGLE_ADS_CLIENT_ID/CLIENT_SECRET/REFRESH_TOKEN → short-lived access token via `https://oauth2.googleapis.com/token`, cached in memory until ≤60s before expiry, never returned to the browser, never logged/persisted); **official v25 REST call** `POST https://googleads.googleapis.com/v25/customers/{customerId}/keywordPlanIdeas:generateKeywordHistoricalMetrics` with `Authorization: Bearer <token>` + `developer-token` + `login-customer-id` ONLY when `GOOGLE_ADS_LOGIN_CUSTOMER_ID` is configured; geo `geoTargetConstants/2840` (USA), language `languageConstants/1000` (English), network `GOOGLE_SEARCH`; customer IDs normalized without hyphens. The Phase-4G hardcoded dead-end is REMOVED — when the credential set is configured the route actually performs the call (exercised against mocks). Error classification: `OAUTH_REFRESH_FAILED`, `AUTH_FAILED`, `CUSTOMER_NOT_ENABLED`, `INVALID_CUSTOMER_ID`, `DEVELOPER_TOKEN`, `RATE_LIMITED`, `VALIDATION`, `GOOGLE_SERVER_ERROR` with safe scrubbed messages + Google `request-id` preserved. **Strict keyword limit:** a 6-keyword request → HTTP 400 `MAX_KEYWORDS_EXCEEDED` (never silently truncated; the server checks the full unique count, the client may still proactively cap generated requests at 5).

**PART B — OFFICIAL V25 RESPONSE PARSER (done).** Primary contract `results[] { text, closeVariants, keywordMetrics { avgMonthlySearches, competition, competitionIndex, lowTopOfPageBidMicros, highTopOfPageBidMicros, monthlySearchVolumes[] { year, month, monthlySearches } } }` (camelCase primary, snake_case secondary — `keyword`/`keywordText`/`keyword_historical_metrics` shapes are explicitly secondary compat only). Monthly history keeps `{ year, month, monthlySearches }` — month is the **MonthOfYear enum name** (e.g. `AUGUST`), never treated as YYYY-MM; a display `YYYY-MM` is derived via an explicit `MONTH_OF_YEAR` enum mapping. Null `monthlySearches` stays null/unknown (never invented zero). Bids are micros → USD. Tests A–E cover each.

**PART C — BROWSER ENV DETECTION REMOVED (done).** `marketDemand.ts` no longer contains `process.env`/`googleAdsConfigured()`/synchronous `configured`. The browser NEVER determines Google configuration — the server route is the only authority for `configured / not_configured / online / offline`. New `GoogleAdsServerDemandAdapter`: real path `ProductScout → /api/market-demand/google-ads (admin JWT) → server OAuth → Google Ads v25`; it sends only keywords/market/language and receives structured metric facts. `getStatus()` is server-derived. No Google secret ever enters client code.

**PART D — PRODUCTSCOUT WIRING (done).** The normal ProductScout MI flow now passes `demand: new GoogleAdsServerDemandAdapter({ getToken: getAccessToken })` into `runMarketIntelligenceJob` — no separate hidden harness. `MarketIntelligenceOptions.demandKeywords?: string[]` added (default `[query]`, max 5, caller-supplied deterministically — never invented by DeepSeek). When Google is not configured the server answers `not_configured` and ZERO external calls occur.

**PART E — STRUCTURED FACTS PRIMARY (done).** `DemandCollectionResult` now carries `requestedKeywords[]`, `returnedResults[]` (structured official facts), `keywordsRequested` (actual normalized request count), `keywordsReturned` (actual Google result count — close-variant dedupe may make returned < requested), `errorCode`, `requestId`, `signals[]` GENERATED from the structured facts. The reverse-summary parsing (`demandMetricsFromSignals`) is REMOVED — aggregates derive from `returnedResults` directly, never from human summary strings. Engine persists `demandRequestedKeywords`, `demandReturnedResults`, `demandErrorCode`, `demandRequestId` + the existing aggregate fields; the result `demand` block exposes them.

**PART F — CACHING HONESTY (done).** The request cache moved into the SERVER Google Ads layer as an in-memory **SERVER INSTANCE CACHE** (labeled honestly — NOT a durable monthly cache; `clearGoogleAdsServerCache()` for tests). Plus **max ONE historical-metrics request per MI job execution**: the adapter serves one collect() per instance and the engine calls collect exactly once per job (test proves outboundCalls = 1). No DB migration added for caching.

**PART G — HEALTH SEMANTICS (done).** `health` returns `not_configured` (env missing) / `configured` (env present, no live probe) — **ONLINE is only reported after a successful owner-authorized Google API request**, never from env presence alone. Google `request-id` is persisted when returned; auth headers/tokens are never persisted.

**Tests:** `npm test` ✅ **388 passed + 1 skipped (live harness)** — +26 new: 18 server-lib tests (official v25 parse A–E, customer-id N, required-config H, version V, OAuth-then-one-call K, request body O/P/Q, manager/direct login-customer-id L/M, token cache K2, error classification U, instance-cache honesty, close-variant dedupe F), 6 route tests (strict >5 rejection G, exactly-5 not_configured G2, not-configured zero-outbound J/J2, USA/English validation, admin-JWT 401), 2 engine tests (MI passes demand adapter with structured facts primary R, no-adapter not_configured). Existing Phase 4G H–O tests updated to the server-backed contract. `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅ · bundle scan clean (no Google secrets, no fixture values; only env-NAME config references).

**Security:** NO real Google credentials added; no `.env`/Vercel changes; no `VITE_GOOGLE_*`; no browser secret access; no DB secret storage; CJ credential unchanged; Production untouched (main `b62090f`).

**Status:** **GOOGLE ADS LIVE PATH CODE = READY · OFFICIAL V25 RESPONSE PARSER = PASS · PRODUCTSCOUT DEMAND WIRING = PASS · BROWSER ENV SECRET DETECTION = REMOVED · LIVE GOOGLE CALLS = 0 · CJ POINTS = 0 · DEEPSEEK = 0 · HIGGSFIELD = 0 · main = `b62090f` (untouched).**

## PHASE 4G.1A — FINAL GOOGLE ADS REST CONTRACT FIX (2026-08-18)

**Mission:** small correctness closure only — align the Google Ads path with the official v25 REST custom-method HTTP binding and proto-JSON body. NO new architecture; NO live calls; NO credentials.

**§1 — REST ENDPOINT FIXED (done).** The path builder now uses the official custom-method binding `POST https://googleads.googleapis.com/v25/customers/{customerId}:generateKeywordHistoricalMetrics` — the method binds DIRECTLY to the customer resource. The previous `/customers/{id}/keywordPlanIdeas:generateKeywordHistoricalMetrics` sub-resource path is removed (it is not the REST binding for this method). The URL builder is centralized in `generateKeywordHistoricalMetrics`; regression tests assert the EXACT URL and that `/keywordPlanIdeas:` never appears (tests A+B).

**§2 — REST/PROTO JSON BODY FIXED (done).** The request body now uses Google REST/proto JSON lower-camel field names: `{ keywords, geoTargetConstants: ["geoTargetConstants/2840"], keywordPlanNetwork: "GOOGLE_SEARCH", language: "languageConstants/1000" }`. `customer_id`/`customerId` is NOT sent redundantly in the body — the customer ID lives in the URL path only (tests C+D). USA 2840 / English 1000 / GOOGLE_SEARCH unchanged (test O+P+Q).

**§3 — STRICT CUSTOMER-ID VALIDATION (done).** `normalizeGoogleAdsCustomerId` now accepts ONLY the valid Google Ads syntax — `1234567890` or `123-456-7890` (3-3-4 hyphenated) — then strips hyphens to digits. Arbitrary letters/symbols+digits (e.g. `abc-1234567890`, `12X-456-7890`, `123.456.7890`) are REJECTED (null) instead of being silently coerced into a plausible-looking id. Same rule applies to `GOOGLE_ADS_LOGIN_CUSTOMER_ID`. Tests E+F.

**§4 — HEALTH SEMANTICS TRUTHFUL (done).** `health` reports only `not_configured` (required env missing) or `configured` (env present) — a presence probe. The previous "ONLINE is reported after a successful request" claim is removed from the route detail text because online state is NOT actually persisted; a successful `historical-metrics` call reports `status: 'success'` instead. No new state/database machinery.

**Tests:** `npm test` ✅ **393 passed + 1 skipped (live harness)** — +5 (exact URL A+B, lower-camel body C+D, constants O+P+Q consolidated, invalid-customer rejection E, valid hyphenated F, mocked full-path parse G; K updated to the exact URL). `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅ · bundle scan clean.

**Security/Live:** NO real Google credentials; no `.env`/Vercel changes; no `VITE_GOOGLE_*`; CJ credential unchanged; Production untouched (`main b62090f`). **LIVE GOOGLE CALLS = 0.**

**Status:** **GOOGLE ADS REST CONTRACT = PASS · LIVE GOOGLE CALLS = 0 · READY FOR OWNER PREVIEW CREDENTIALS = YES (all tests pass) · GOOGLE ADS LIVE DEMAND = DEFERRED (owner billing decision — NOT a launch blocker) · main = `b62090f` (untouched).**

## PHASE 4H — ELEVATED DOG SOFA BED EVIDENCE PASS + GOOGLE ADS DEFERRED (2026-08-18)

**Owner business decision (this phase):** Google Ads is **DEFERRED / NOT A LAUNCH BLOCKER** — the Google Ads account requires payment/billing setup before the advertiser account can proceed. No payment/card/campaign/spend. The Google Ads code built in 4G/4G.1/4G.1A is **PRESERVED / READY FOR LATER**. Current priority: **PRODUCT → QUALIFICATION → LISTING → CHECKOUT → SALES**.

**Mission executed (one serious, honest pass — no new architecture):** researched the owner-priority concept **elevated dog sofa bed** using ONLY the existing persisted Phase 4F CJ seeds (durable seed job `b01c2362-…`, 40 records — **NO new CJ seed run, NO new CJ calls**), applied the unchanged gates, and stopped honestly.

**Concept clustering honesty fixes (small, targeted — exposed by this pass):**
1. **Plural normalization** in `conceptKey` — "door"/"doors", "bag"/"bags", "cage"/"cages" now share one concept token so real variant clusters merge (regression test: `24-30 Inch Single Door Dog Cage` + `A Dog Cage With Two Doors` → ONE concept).
2. **Clarity proxy fixed** — replaced the raw title-length band (12–120 chars) with a normalized token-count band (2–24). A real Phase 4F CJ title (`Elegant Rectangular Pet Bed … Durable Elevated Dog Sofa Bed, …` — >120 chars, SEO-verbose) is a perfectly clear single product; the old proxy unfairly dropped it from selection.
3. **Researchability fixed** — was `tokens 2–7`, which contradicted `seedConceptSearchQuery`'s own 7-token query cap; now any concept with ≥2 tokens is researchable.
Consequence: the sofa-bed concept's suitability rose 10 → **14/15** (complete: pet-relevant, clear, priced, imaged, US-inventory, safe, researchable) and re-clusters with its 3 color variants. Existing Phase 4G equality invariants (member-count/listedNum independence) unchanged. 2 new regression tests.

**LIVE proof (ONE controlled market-evidence pass, real preview `/api/fetch-page` + `/api/ai/generate`, real Supabase DB, temp admin created + deleted; `RUN_LIVE_CJ_PROOF=1`, `-t "market-evidence-only pass"`):**
- **Concept:** `Elegant Rectangular Pet Bed … Elevated Dog Sofa Bed …` — 3 real CJ records, suitability 14 (owner-priority selection from the persisted seeds).
- **Query (CJ-learned vocabulary):** `elegant rectangular pet bed small medium sized` — and the vocabulary is VERBATIM the retail product name: discovery surfaced **the exact product** on Amazon / Target / Wayfair / Walmart / Lowes (10 real URLs; the CJ seed → retail identity match is the discovery breakthrough).
- **Evidence pack:** 8 exact PDPs selected (round-robin, ≤2/domain) · **4 usable extracts** (3 failed: 2× HTTP 502 proxy, 1× blocked/rate-limited) · **3 independent domains** · **price evidence 1** (only the Amazon extract yielded a real price — Target/Wayfair/Lowes PDPs render prices via JS / resist snapshot extraction) · **availability evidence 4 (3 available, 1 unavailable)** · **ratings 3 pages, review counts 2 pages (0 numeric totals observed — never republished)** · **identity: 4 pages with explicit identity evidence, 4 exact-identity groups** (same product across retailers — strong, honest multi-retailer confirmation).
- **Gate:** evidence quality = **INSUFFICIENT** — the ONLY missing requirement is **price evidence (1 of 4 extracts, need ≥3)**. Diagnostic deterministic score **68/100** (up from 28–33 on generic queries — the CJ vocabulary targets the exact product). **Qualifying Market Score = NULL (fail-closed)** · **DeepSeek calls = 0 (gate saved the credit)** · **conditional CJ qualification run NOT started** (`evidence-insufficient`).
- **CJ research points = 0 · Google Ads = 0 calls (DEFERRED) · winner = NONE.** MI job `9f671da4-…` persisted (marketScore null, qualificationEligible false, diagnostic 68).
- **Exact next gap (honest):** the bottleneck is now PRICE EXTRACTION on retailer PDPs, not discovery — the exact product is confirmed live on 5+ US retailers with matching identity and availability; the evidence gate needs 3 extractable retailer prices (a fetch layer that returns retailer-rendered prices, or additional price-rich PDP sources, closes it). Per the owner instruction, this phase ran ONE pass and stopped — no score fishing, no category looping, no forced winner.

**Tests:** `npm test` ✅ **395 passed + 2 skipped (live harness)** (2 new clustering/suitability regression tests; the new live market-evidence-only harness test is guarded like the others) · `npx tsc --noEmit` ✅ 0 errors · `npm run build` ✅. **Security:** no new credentials; CJ key unchanged (Preview, server-side); Google Ads deferred with zero calls; no `.env`/Vercel changes; Production untouched (`main b62090f`).

**Status:** **GOOGLE ADS LIVE DEMAND = DEFERRED (billing — NOT a launch blocker) · GOOGLE ADS CODE = PRESERVED / READY FOR LATER · CONCEPT CLUSTERING HONESTY = PASS (plural merge + long-title clarity/researchability) · ELEVATED DOG SOFA BED EVIDENCE PASS = EXECUTED (4 PDPs / 3 domains / 4 availability / 1 price) · MARKET GATE = FAIL-CLOSED (evidence insufficient, diagnostic 68 ≠ qualifying) · DEEPSEEK CALLS = 0 · CJ PRODUCT POINTS THIS PHASE = 0 · QUALIFIED WINNER = NONE · HIGGSFIELD = PLAN PENDING / CALLS 0 · main = `b62090f` (untouched).**

## Exact recommended next task

Phase 4F broke the discovery deadlock (CJ seeds → real concepts → first sufficient evidence pack: dog ramp, 5 PDPs / 4 domains / 3 prices). Phase 4G/4G.1/4G.1A hardened CJ accounting/identity and built the Google Ads demand path to REAL-CAPABLE — but the owner has **DEFERRED Google Ads** (the account requires payment/billing setup; no payment/card/campaign; the code is preserved and will be enabled later after sales/revenue). Phase 4H then executed **one serious market-evidence pass for the owner-priority concept `elevated dog sofa bed`** using only the persisted Phase 4F CJ seeds (no new seed run): the CJ-learned vocabulary surfaced the EXACT retail product on 5+ US retailers (Amazon/Target/Wayfair/Walmart/Lowes), the pack reached **4 usable PDPs / 3 independent domains / 4 explicit availability (3 available) / 1 price / 3 rating pages / 4 exact-identity groups** — the gate failed on the SINGLE remaining requirement, **price evidence (1/4 extracts, need ≥3)** — qualifying Market Score stayed NULL (fail-closed), DeepSeek 0, CJ research points 0, winner NONE (diagnostic 68 ≠ qualifying).

**Exact next steps, in order:** (1) close the price-evidence gap for a qualifying pass — the exact product is already identity-confirmed across retailers; the fetch/extract layer needs retailer-rendered prices (or an additional price-rich PDP source) so the gate can reach 3 real prices; (2) only when a grounded Market Score ≥60 is persisted with a recommendation may the first 250-pt market-grounded CJ qualification run start (durable ledger + provenance gates proven end-to-end); (3) ship the real DB-driven storefront (luxedge-v2 `catalog.ts`) to production via a separately-approved release; (4) Higgsfield plan later; (5) before Google Merchant Center / Ads (a later phase after Google Ads billing is enabled): resolve the production footer payment-method badges (Part F TODO). Google Ads live demand remains DEFERRED — do not add the `GOOGLE_ADS_*` env set until the owner enables billing. The live harness (`RUN_LIVE_CJ_PROOF=1`, incl. the market-evidence-only pass for the sofa-bed concept) re-runs the controlled flow against the real preview. `main` = `b62090f` (production hotfix live); luxedge-v2 advances the product→sales path only.
